# redis-uya v0.9.3 performance analysis

Generated: 2026-07-17 09:10:00 +0800

## Scope

- Host: Intel Xeon E5-2698 v4, 4 visible CPUs, Linux x86_64.
- Workload: single connection, pipeline 1, AOF enabled without explicit fsync.
- Redis baseline: same host, `appendonly yes`, `appendfsync no`, `save ""`.
- redis-uya release command: `CFLAGS='-std=c99 -O3 -DNDEBUG -fno-builtin -fomit-frame-pointer' ./uya/bin/uya build build/app_workspace/main.uya -o build/redis-uya --c99 -e -O3`.
- The current benchmark script still hard-codes `build_mode=debug` in raw output. The release command above is the source of truth for the release samples in this report; fixing benchmark build metadata is the next required item.

## Confirmed Root Cause

The replication backlog previously called `sds_range()` every time its logical size exceeded the 1MiB limit. After the buffer became full, every write allocated and copied almost the complete backlog. A 1KiB `SET` therefore introduced roughly 1MiB of extra copying per request during the steady state.

The optimized backlog keeps a logical start offset, advances it in O(1) when old bytes expire, and compacts only after the discarded prefix reaches a threshold. `histlen`, first offset and PSYNC slice semantics remain unchanged.

## Before And After

Release build, 1000 measured requests per case, 50 warmup requests:

| Case | Before req/s | After req/s | Throughput change | Before p99 us | After p99 us | p99 change |
|------|-------------:|------------:|------------------:|--------------:|-------------:|-----------:|
| `ping` | 10006 | 10467 | 1.05x | 175 | 255 | 1.46x |
| `set_16b` | 6049 | 8112 | 1.34x | 284 | 233 | 0.82x |
| `get_16b` | 12131 | 12764 | 1.05x | 133 | 127 | 0.95x |
| `set_1024b` | 1468 | 4078 | 2.78x | 977 | 442 | 0.45x |
| `get_1024b` | 12113 | 10398 | 0.86x | 93 | 169 | 1.82x |

The short sample is suitable for confirming the targeted write-path effect, not for accepting unrelated latency changes. Host jitter remains visible in the read-only cases.

Release build, 5000 measured requests per case, 200 warmup requests after the optimization:

| Case | redis-uya req/s | Redis req/s | Throughput ratio | redis-uya p99 us | Redis p99 us |
|------|----------------:|------------:|-----------------:|-----------------:|-------------:|
| `ping` | 10469 | 27258 | 0.38x | 175 | 80 |
| `set_16b` | 8464 | 16416 | 0.52x | 255 | 105 |
| `get_16b` | 12280 | 18313 | 0.67x | 191 | 98 |
| `set_1024b` | 2041 | 13456 | 0.15x | 1367 | 127 |
| `get_1024b` | 11768 | 19240 | 0.61x | 131 | 89 |

Compared with the recorded 2026-07-11 debug baseline, `SET 1KiB` rises from 180 to 2041 req/s, or about 11.3x. This does not yet meet the Redis target.

## Optimization Queue

| Priority | Area | Evidence | Next action |
|----------|------|----------|-------------|
| P0 | Release benchmark contract | Project targets currently build with C `-O0 -g`; benchmark output always reports `debug` | Add explicit debug/release build targets and derive report metadata from the selected build mode |
| P0 | AOF large-record path | Records above 512B flush the shared buffer and call `write(2)` directly for every request | Benchmark buffered 1KiB AOF records against direct writes while preserving append order and error handling |
| P0 | Backlog physical storage | Logical trimming removes per-request 1MiB copies, but periodic compaction and allocator residency remain visible in the 5000-request sample | Move toward a fixed-size circular or segmented backlog so steady-state append never reallocates the full history |
| P1 | RESP parse ownership | Each request builds and frees a parsed value tree before dispatch | Add allocation/copy counters and introduce borrowed bulk slices for the common command-array path |
| P1 | String write allocation | `SET` creates a new object and SDS value, inserts a copied key, and updates WATCH metadata | Profile object allocation, value copy, dict insert and watch-version work separately; reuse storage on overwrite where semantics allow |
| P1 | Event-loop round trip | Release `PING` remains at 0.38x Redis and p99 is 2.19x Redis | Measure read, parse, dispatch, encode, async frame and epoll interest-update time with internal counters |
| P2 | Pipeline and concurrency | Current matrix uses one connection and pipeline 1 | Add pipeline and multi-connection matrices before claiming Redis parity or superiority |

## Target Gates

Redis superiority must be claimed only after a release-build matrix shows at least 1.10x Redis throughput with p99 no worse than Redis for the selected core cases, under identical durability, client concurrency, pipeline, dataset and hardware settings. The current result is a material write-path improvement, not completion of that target.
