# redis-uya Definition of Done

> 版本: v0.9.3-dev
> 日期: 2026-07-31
> 状态: 下列条目保留历史里程碑证据；当前 Uya 1.0 规范回退后的构建、完整单测、完整集成、redis-cli、release 构建和性能 guard 通过，主线处于 `v0.9.3` 核心缺口补齐、性能收敛与运维第一批

## 1. 目标

本页用于把 `redis-uya` 的阶段能力映射到明确测试、验证脚本或 benchmark 证据。

## Uya 1.0 工具链同步（2026-07-22）

| DoD 项 | 证据 |
|--------|------|
| 上游 `1.0` 分支工具链按源码重新构建并成套同步：`bin/uya` 静态启动器与 `bin/cmd/build` 实际编译器均返回 `v0.9.9`，哈希和尺寸已固化 | `uya/bin/uya`、`uya/bin/cmd/build`、`docs/redis-uya-uya-sync-2026-07-22.md` |
| 上游 `1.0` 分支已直接回退到 `f54bd7bf`，与 `origin/1.0` 一致；违反导出全局裸 C ABI 规范的两个未推送提交已从分支历史移除 | `git -C ../uya rev-parse HEAD`、`git -C ../uya rev-list --count origin/1.0..HEAD` |
| redis-uya 不再用 `export const/var` 表达项目内部模块 API；内部全局量保持私有，跨模块访问统一通过导出函数，并由源码契约检查防止回归 | `make verify-uya-source-contract`、`src/cluster/`、`src/command/`、`src/memory/allocator.uya`、`src/util/log.uya` |
| 当前项目完成无警告 debug 构建、完整单测、完整集成、redis-cli smoke、release 构建与性能回归 guard | `make build`、`make test`、`make test-integration`、`make test-redis-cli`、`make benchmark-v0.9.3-release` |

重要说明：

- 本页下方的 `v0.1.0` ~ `v0.9.1` 条目首先是“历史阶段曾经落地过什么”的证据索引。
- 它们不能自动等价为“当前 `HEAD` 已重新验证通过”。
- 当前 `HEAD` 的真实性状态先看 [审计报告（2026-05-16）](./redis-uya-audit-2026-05-16.md)。

基础一键验证入口：

```bash
bash scripts/verify_definition_of_done.sh
```

补充说明：

- `tests/integration/long_run_smoke.py` 为 30 分钟长时运行验证，不纳入默认一键脚本
- `benchmarks/v0.1.0.md` 记录同机 Redis 基线与 `floor/target/stretch` 判定
- `benchmarks/v0.8.0-performance.md` 记录 `PING/SET/GET` 热路径矩阵、同机 Redis 对照与回归阈值基线
- `benchmarks/v0.8.0-gap-report.md` 记录 v0.8.0 相对 Redis 的差距矩阵与后续优化队列
- `benchmarks/v0.8.1-performance.md` 保留历史 `v0.8.1` guard 输出；当前 HEAD 的性能验收使用 `benchmarks/v0.9.3-release-performance.md` 和对应不可变基线
- `benchmarks/v0.9.3-performance.md` 记录 2026-07-11 当前 `HEAD` 的性能复测；报告已生成，但 guard miss，不能作为当前性能绿态证据
- `docs/redis-uya-release-v0.8.0.md` 与 `docs/redis-uya-test-report-v0.8.0.md` 固化 v0.8.0 封版边界和实际验证结果
- `docs/redis-uya-release-v0.8.1.md` 与 `docs/redis-uya-test-report-v0.8.1.md` 固化 v0.8.1 封版边界和实际验证结果
- 一键验证脚本会把临时 benchmark 输出写入 `build/`，避免覆盖已记录的基线报告
- 一键验证脚本包含 `git diff --check`，用于检查本次工作区差异的基础格式问题
- 本页同时记录 `v0.1.0` 发布证据，以及后续 `v0.2.0+` 已在主线落地的能力证据

## 2. 当前 HEAD 复核状态（2026-07-31）

| 项目 | 当前结果 | 说明 |
|------|----------|------|
| `make test` | `PASS` | 单元层仍可作为基础回归入口 |
| `make test-integration` | `PASS` | `maxmemory` / 压力 / 淘汰策略相关回归已按当前实现重新校准 |
| `make benchmark-v0.9.3-release` | `PASS` | 使用 release 构建的 50K 基线；零拷贝 writev 首次直写后的不可变与 current 五项绝对吞吐、normalized 和 p99 guard 全部通过；当前 Redis 比值为 `0.95x/1.24x/0.99x/1.26x/1.14x`，GET 1KiB 达到本轮 1.10x 吞吐与 p99 门槛，PING 与 GET 16B 仍未达到 |
| `bash scripts/verify_definition_of_done.sh` | `PASS` | 当前一键验证执行 release benchmark，不再用历史 debug 基线判定当前 HEAD |
| `COMMAND*` 真实性 | `PASS` | 运行时 `COMMAND*` 已按真实执行面隐藏未实现命令，并补齐当前 `CLIENT/CONFIG` 已实现子命令的矩阵状态 |
| 版本号一致性 | `PASS` | banner、`HELLO`、`INFO server`、README、DoD 和相关测试断言已统一到 `v0.9.3-dev` |

后续历史章节从这里开始顺延，保留原编号不代表当前 `HEAD` 已重新验收。

## 3. `v0.1.0-alpha`

| DoD 项 | 证据 |
|--------|------|
| `PING/GET/SET/DEL/EXISTS` 可通过 TCP smoke 交互 | `tests/integration/smoke_tcp.py` |
| SDS、Dict、Object、Engine 有单元测试 | `tests/unit/*_test.uya` |
| SDS 1MB 追加与布局说明完成 | `tests/unit/storage_sds_test.uya`、`docs/redis-uya-sds-layout.md` |
| Dict 渐进 rehash 可手动推进 | `tests/unit/storage_dict_test.uya` |
| 错误路径不会崩溃 | `tests/unit/*_test.uya` 中错误路径用例 |
| 100ms server cron 可触发主动过期扫描 | `tests/unit/storage_engine_test.uya`、`tests/unit/server_test.uya` |

## 4. `v0.1.0-beta`

| DoD 项 | 证据 |
|--------|------|
| AOF 重启恢复正确 | `tests/integration/persistence_aof.py` |
| AOF 追加、回放、损坏文件失败路径有单元证据 | `tests/unit/persistence_aof_test.uya` |
| 连接状态机与服务循环稳定 | `tests/integration/smoke_tcp.py`、`tests/integration/idle_client.py` |
| 空闲客户端不会阻塞其他客户端 | `tests/integration/idle_client.py` |
| `redis-cli` 可完成基础交互 smoke | `tests/integration/redis_cli_smoke.sh` |
| 慢读客户端不会因写回背压导致其他客户端停顿 | `tests/integration/slow_reader.py` |
| 错误响应与协议错误基础兼容 | `tests/integration/error_compat.py` |
| Python 子集集成测试通过 | `make test-integration` |
| 长时运行 smoke 完成 | `tests/integration/long_run_smoke.py`、`docs/redis-uya-release-v0.1.0.md` |

## 5. `v0.1.0`

| DoD 项 | 证据 |
|--------|------|
| 同机 Redis 基线可复现 | `scripts/benchmark_v0_1_0.py`、`benchmarks/v0.1.0.md` |
| `PING/SET/GET` benchmark 可生成 | `scripts/benchmark_v0_1_0.py`、`benchmarks/v0.1.0.md` |
| benchmark 基线可复现 | `benchmarks/v0.1.0.md` |
| Redis 对照口径明确 | `docs/redis-uya-benchmark-format.md` |
| 发布文档齐全 | `docs/redis-uya-release-v0.1.0.md`、`docs/redis-uya-quickstart.md`、`docs/redis-uya-api.md`、`docs/redis-uya-architecture.md` |

## 6. `v0.2.0`

| DoD 项 | 证据 |
|--------|------|
| Hash 最小对象可创建、写字段、读字段、覆盖字段并正确释放 | `tests/unit/storage_object_test.uya` |
| `HSET/HGET` 命令子集在单元与基础 TCP smoke 中可用 | `tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py` |
| List 最小对象可创建、头插、头弹出、范围读取并正确释放 | `tests/unit/storage_object_test.uya` |
| `LPUSH/LPOP/LRANGE` 命令子集在单元与基础 TCP smoke 中可用 | `tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py` |
| Set 最小对象可创建、去重插入、删除成员、枚举成员并正确释放 | `tests/unit/storage_object_test.uya` |
| `SADD/SREM/SMEMBERS` 命令子集在单元与基础 TCP smoke 中可用 | `tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py` |
| ZSet 最小对象可创建、按 score 排序读取、更新成员 score、删除成员并正确释放 | `tests/unit/storage_object_test.uya` |
| `ZADD/ZRANGE/ZREM` 命令子集在单元与基础 TCP smoke 中可用 | `tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py` |
| `SCAN` 最小语义可用：cursor 返回、`COUNT` 子集、按稳定顺序迭代非过期 key | `tests/unit/storage_engine_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py` |
| `INFO` 支持 `server/clients/memory/stats/keyspace` section 子集，`CONFIG GET` 支持最小配置查询 | `tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_cli_smoke.sh` |
| 100ms cron 使用主动过期采样循环，过期比例高时会继续多轮清理 | `tests/unit/storage_engine_test.uya`、`tests/unit/server_test.uya` |

## 7. `v0.3.0`

| DoD 项 | 证据 |
|--------|------|
| 项目内 RDB 子集已覆盖 String/Hash/List/Set/ZSet 与绝对过期时间 save/load | `tests/unit/persistence_rdb_test.uya`、`tests/integration/persistence_bgsave.py` |
| `SAVE` 命令可写出当前五类对象的 RDB 快照 | `tests/unit/command_executor_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_cli_smoke.sh` |
| `BGSAVE` 通过真实 `fork/waitpid` 子进程在后台写出 RDB 快照，并可在去掉 AOF 后仅靠 RDB 恢复 | `tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/server_test.uya`、`tests/integration/persistence_bgsave.py` |
| 服务启动时先加载最小 RDB，再回放 AOF 完成混合恢复 | `tests/unit/server_test.uya`、`tests/integration/persistence_rdb_aof.py` |
| `BGREWRITEAOF` 通过真实子进程后台 rewrite 与父进程增量缓冲合并，可生成可回放 AOF 并在重启/崩溃后恢复 | `tests/unit/persistence_rewrite_test.uya`、`tests/unit/server_test.uya`、`tests/integration/persistence_aof.py`、`tests/integration/persistence_crash_matrix.py` |
| Python 客户端风格子集覆盖更多命令与控制面 | `tests/integration/redis_py_subset.py` |
| RDB 损坏/截断与 AOF 损坏/截断在单元与进程级恢复路径上都有证据 | `tests/unit/persistence_rdb_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/server_test.uya`、`tests/integration/persistence_corruption.py` |
| 进程级崩溃恢复矩阵覆盖 AOF-only、rewrite in-progress、rewrite completed 三条路径 | `tests/integration/persistence_crash_matrix.py` |
| 持久化 benchmark 可生成并落盘 | `scripts/benchmark_persistence_v0_3_0.py`、`benchmarks/v0.3.0-persistence.md` |

## 8. `v0.4.0`

| DoD 项 | 证据 |
|--------|------|
| 复制角色与状态机可用：支持 master/slave 角色切换、`REPLICAOF` / `SLAVEOF` alias、`INFO replication`、`CONFIG GET replicaof/masterauth` | `tests/unit/config_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/server_test.uya`、`tests/integration/replication_role_state.py` |
| `PSYNC / backlog` 最小闭环可用：master 维护复制积压缓冲区，`PSYNC ? -1` 返回 `FULLRESYNC`，匹配 replid+offset 时返回 `CONTINUE` | `tests/unit/replication_backlog_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/replication_psync_backlog.py` |
| `SYNC` legacy full-sync partial 可用：master 角色返回 RESP Bulk String 包裹的项目内 RDB 快照，replica 角色拒绝执行，并进入 `COMMAND*` 可见面；当前不实现后续长连接命令流，真实 replica 仍优先使用 `PSYNC` | `src/command/router.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_aa.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/replication_psync_backlog.py`、`tests/integration/command_introspection.py`、`make test`、`make test-integration` |
| `REPLCONF` no-op 握手兼容面可用：常见 `CAPA` / `ACK` / 空参数形态返回 `OK`，不改变复制状态，不进入 AOF 或 replication backlog，并通过 redis-cli/redis-py/TCP/`COMMAND*` smoke 固化当前 partial 边界 | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| replica 侧全量同步可用：`REPLICAOF` 后可向 master 发起 `PSYNC ? -1`，拉取 RDB 快照并落当前库 | `tests/unit/persistence_rdb_test.uya`、`tests/integration/replication_full_sync.py` |
| replica 侧增量同步可用：connected 状态下可周期性拉取 backlog delta 并回放到本地库 | `tests/integration/replication_incremental_sync.py` |
| 复制心跳可用：replica 会周期性 `PING` master，掉线后回退到 `configured` 并在 master 恢复后重新同步 | `tests/integration/replication_heartbeat.py` |
| 主从一致性 smoke 覆盖当前五类对象的 full sync + incremental 复制 | `tests/integration/replication_consistency.py` |
| 复制 benchmark 可生成并落盘 | `scripts/benchmark_replication_v0_4_0.py`、`benchmarks/v0.4.0-replication.md` |

## 9. `v0.5.0`

| DoD 项 | 证据 |
|--------|------|
| RESP3 最小协议闭环可用：支持 `HELLO 2/3` 连接级协议切换、RESP3 Null/Boolean/Map 解析、RESP3 Null 回复和不支持协议版本错误路径 | `tests/unit/network_protocol_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py` |
| `MULTI/EXEC/DISCARD` 最小事务子集可用：连接级队列、`QUEUED`、`EXEC` 数组回复、`DISCARD` 丢弃、无 `MULTI` 错误路径 | `tests/unit/command_router_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/error_compat.py` |
| `WATCH/UNWATCH` 最小事务观察子集可用：按键版本跟踪、变更后 `EXEC` 返回 Null Array、Redis 7.4+ `CLIENT INFO/LIST watch` 去重 key 计数、WATCH 版本失配时 `flags=d`、`UNWATCH` 清空观察集并恢复 `watch=0 flags=N`、`WATCH/UNWATCH` in-transaction 错误路径 | `tests/unit/command_router_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py` |
| `PUBLISH/SUBSCRIBE/UNSUBSCRIBE` 最小 Pub/Sub 闭环可用：连接订阅注册、跨连接发布推送、发布返回订阅者数量、取消订阅后不再收到消息 | `tests/unit/command_router_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/pubsub_smoke.py` |
| `CLIENT` / `CONFIG` 控制面兼容子集可用：`CLIENT ID/GETNAME/GETREDIR/SETNAME/INFO/LIST/SETINFO/HELP/REPLY/UNBLOCK`、`HELLO AUTH/SETNAME`、`CONFIG GET/HELP/RESETSTAT` | `tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/smoke_tcp.py` |
| `v0.5.0` 兼容性回归覆盖协议与控制面组合路径：RESP3 Null、`HELLO SETNAME`、WATCH 中止、事务内控制命令错误、RESP3 Pub/Sub Push、控制面查询 | `tests/integration/v0_5_compat.py`、`tests/integration/error_compat.py`、`tests/integration/redis_py_subset.py` |

## 10. `v0.6.0`

| DoD 项 | 证据 |
|--------|------|
| `maxmemory` noeviction 基线可用：启动参数可设置最大内存，`CONFIG GET/INFO memory` 可观测，超预算增量写命令返回 OOM 且不落库 | `tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/maxmemory_noeviction.py` |
| `allkeys-lru` 运行时淘汰基线可用：对象记录访问时间，读写触碰更新 LRU，超预算写入可淘汰最久未访问 key 后继续执行，`CONFIG GET/INFO memory` 可观测策略 | `tests/unit/storage_engine_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/maxmemory_allkeys_lru.py` |
| `allkeys-lfu` 运行时淘汰基线可用：对象记录访问计数，读写触碰递增 LFU，超预算写入可淘汰访问次数最低 key 后继续执行，同频次用 LRU 打破平局 | `tests/unit/storage_engine_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/maxmemory_allkeys_lfu.py` |
| `volatile-*` 运行时淘汰基线可用：`volatile-lru` / `volatile-lfu` / `volatile-ttl` 只从带 TTL 的 key 中选候选，永久 key 不被 volatile 策略淘汰 | `tests/unit/storage_engine_test.uya`、`tests/integration/maxmemory_volatile_policies.py` |
| 内存统计完善：allocator 记录当前使用、峰值、累计分配、累计释放、累计分配次数和当前活跃块数，`INFO memory` 可观测这些字段 | `tests/unit/memory_allocator_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/memory_info_stats.py` |
| Slab allocator 基线可用：`redis_malloc/free/realloc` 内部对 16B 到 1KB 小对象做分级 freelist 缓存，缓存块数、缓存字节数和复用次数可通过 `INFO memory` 观测 | `tests/unit/memory_allocator_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/memory_info_stats.py` |
| 内存压力与淘汰回归可复现：真实 TCP 循环写入覆盖 noeviction OOM、allkeys-lru、allkeys-lfu 与 volatile-ttl 压力路径，并校验新写入存活、旧/冷/近过期 key 被淘汰、永久 key 不被 volatile 策略淘汰 | `tests/integration/maxmemory_pressure.py` |

## 11. `v0.7.0`

| DoD 项 | 证据 |
|--------|------|
| Cluster 槽位模型可用：按 Redis Cluster CRC16 计算 `0..16383` 槽位，支持 `{hash-tag}` 子串选择，空 tag、缺失右括号、多个 tag 与嵌套大括号边界行为有单元证据 | `src/cluster/slots.uya`、`tests/unit/cluster_slots_test.uya` |
| 节点元数据模型可用：支持 40 字节 node id、本地 master 默认构造、host/port/bus port、master/replica 角色、flags、config epoch 与 connected 状态，并覆盖显式元数据和角色名边界 | `src/cluster/node.uya`、`tests/unit/cluster_node_test.uya` |
| 最小集群拓扑可用：默认单节点拓扑拥有 16384 个槽，可添加远端节点、按槽位范围重新分配 owner、按 slot/key 查询 owner，并覆盖非法 slot、节点查找和容量限制 | `src/cluster/topology.uya`、`tests/unit/cluster_topology_test.uya` |
| `CLUSTER` 最小命令接口可用：支持 `KEYSLOT`、`INFO`、`NODES`、`SLOTS`、`HELP`，真实 TCP smoke 校验 hash tag 槽位、单节点拓扑输出、node id 长度和帮助列表 | `tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/cluster_smoke.py` |
| `ASKING` / `READONLY` / `READWRITE` standalone-error 可用：命令进入运行时路由和 `COMMAND*` 可见面，当前单节点 profile 统一返回 cluster support disabled，不维护 Redis Cluster 客户端连接态 | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/cluster_smoke.py`、`tests/integration/command_introspection.py` |
| `MOVED` / `ASK` 重定向路径可用：服务端持有最小拓扑状态，`CLUSTER MEET` 可注册远端节点，`CLUSTER SETSLOT ... NODE` 可触发稳定远端槽位 `MOVED`，`CLUSTER SETSLOT ... MIGRATING` 可触发迁移态 `ASK`，失败写命令不会进入 AOF/复制追加路径 | `tests/unit/command_executor_test.uya`、`tests/integration/cluster_smoke.py` |
| 集群一致性 smoke 可复现：真实 TCP 进程中校验远端槽位后 `CLUSTER NODES` 槽位范围分裂、`MOVED/ASK` 写命令不落本地库、不进入 AOF，以及 `SETSLOT STABLE` 清除迁移态后恢复本地访问 | `tests/integration/cluster_consistency.py` |

## 12. `v0.8.0`

| DoD 项 | 证据 |
|--------|------|
| 核心 benchmark 矩阵与回归阈值可用：覆盖 `PING`、16B/1KiB `SET`、16B/1KiB `GET`，记录 p50/p95/p99、吞吐、RSS、同机 Redis 对照，并支持用既有报告作为基线判定吞吐和 p99 退化 | `scripts/benchmark_v0_8_0.py`、`make benchmark-v0.8.0`、`benchmarks/v0.8.0-performance.md` |
| `GET` bulk string 零拷贝响应路径可用：64B 及以上命中值在真实 fd 发送路径使用 `writev` 分段发送 RESP 头、对象值 body 和 CRLF，避免把 value body 复制到连接输出缓冲；小 body 保持原路径，避免 syscall 开销导致退化 | `src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`make test`、`make test-integration`、`REDIS_UYA_BENCH_BASELINE=benchmarks/v0.8.0-performance.md REDIS_UYA_BENCH_OUT=build/v0.8.0-zero-copy.md make benchmark-v0.8.0` |
| RESP2/RESP3 顶层批量解析可用：一次扫描可返回多个完整顶层帧、每帧消费长度和完整前缀总消费长度；遇到首帧半包返回 `RespIncomplete`，遇到完整前缀后的半包返回已解析前缀，错误尾包会释放已解析前缀 | `src/network/protocol.uya`、`tests/unit/network_protocol_test.uya`、`make test`、`REDIS_UYA_BENCH_BASELINE=benchmarks/v0.8.0-performance.md REDIS_UYA_BENCH_OUT=build/v0.8.0-batch-resp.md make benchmark-v0.8.0` |
| SIMD 字符串比较与 CRC64 加速可用：新增 `@vector` 16 字节块的 byte-slice 比较/大小写比较工具，命令路由、配置 token、SDS 比较和 Dict key 比较复用该工具；CRC64 更新改为 256 项表驱动，并保留标量路径用于正确性对照 | `src/util/bytes.uya`、`src/util/crc64.uya`、`tests/unit/util_bytes_test.uya`、`tests/unit/util_crc64_test.uya`、`make test`、`make test-integration`、`REDIS_UYA_BENCH_BASELINE=benchmarks/v0.8.0-performance.md REDIS_UYA_BENCH_OUT=build/v0.8.0-simd-crc64.md make benchmark-v0.8.0` |
| `io_uring` 评估可复现且不绑定生产路径：评估脚本记录内核、sysctl、`io_uring_setup` syscall、liburing 探测和建议，报告明确 `production_binding=no`，当前网络事件循环仍保持 epoll 路径 | `scripts/evaluate_io_uring_v0_8_0.py`、`make evaluate-io-uring-v0.8.0`、`benchmarks/v0.8.0-io-uring.md`、`docs/redis-uya-benchmark-format.md` |
| 专用对象池与布局观测可用：`RedisObject` 与 `ListNode` 释放后进入专用 freelist，复用时不触碰通用 Slab 路径；allocator stats 仍按逻辑活跃对象增减，`INFO memory` 暴露对象池缓存、复用计数和布局大小 | `src/storage/object.uya`、`src/memory/allocator.uya`、`src/command/executor.uya`、`tests/unit/storage_object_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/memory_info_stats.py`、`make test`、`make test-integration`、`REDIS_UYA_BENCH_BASELINE=benchmarks/v0.8.0-performance.md REDIS_UYA_BENCH_OUT=build/v0.8.0-object-pool.md make benchmark-v0.8.0` |
| Redis 对照差距报告与优化队列可复现：从 `BENCH_RESULT` 生成每个 case 的吞吐、p99、RSS 比例，输出 `PERF_GAP_RESULT` / `PERF_DEBT_RESULT` 机器可读行，并明确后续 P0/P1/P2 性能债务而不把“超越 Redis”作为单版硬门槛 | `scripts/report_v0_8_0_gaps.py`、`make report-v0.8.0-gaps`、`benchmarks/v0.8.0-gap-report.md`、`docs/redis-uya-benchmark-format.md` |

## 13. `v0.8.1`

| DoD 项 | 证据 |
|--------|------|
| WATCH 版本表懒维护可用：无活跃 WATCH 客户端时普通写命令不维护 `watch_versions`，有 WATCH 客户端时 `SET/DEL/EXPIRE` 仍推进版本并触发事务中止 | `src/storage/engine.uya`、`src/network/connection.uya`、`tests/unit/storage_engine_test.uya`、`tests/unit/network_connection_test.uya`、`make test` |
| Dict 覆盖写单次探测可用：`dict_insert_with_old()` 插入时返回 inserted，覆盖时返回旧值，`set_key_at()` 用该结果释放旧对象 | `src/storage/dict.uya`、`src/storage/engine.uya`、`tests/unit/storage_dict_test.uya`、`tests/unit/storage_engine_test.uya`、`make test` |
| AOF 分层写入可用：512B 以下命令进入 64KiB buffer，较大命令 flush 小缓冲后直接写；flush 在 server cron、客户端关闭、server close 与 BGREWRITEAOF fork 前触发 | `src/persistence/aof.uya`、`src/server.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/persistence_crash_matrix.py`、`tests/integration/cluster_consistency.py`、`make test`、`make test-integration` |
| v0.8.1 性能回归验证可复现：`make benchmark-v0.8.1` 默认以 `benchmarks/v0.8.0-performance.md` 为 guard 基线，输出 `benchmarks/v0.8.1-performance.md`，throughput guard 同时参考绝对历史基线与同机 Redis 归一化比例，五个 case 的吞吐和 p99 guard 当前复跑已通过 | `Makefile`、`scripts/benchmark_v0_8_0.py`、`benchmarks/v0.8.1-performance.md`、`make benchmark-v0.8.1` |

## 14. `v0.9.0`

| DoD 项 | 证据 |
|--------|------|
| Key/Server 第一批只读命令可用：`ECHO`、`TYPE`、`DBSIZE` 在命令路由、执行器、TCP 编解码和 redis-cli/redis-py smoke 中可用 | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key/Server 第二批过期毫秒语义可用：`PEXPIRE`、`PERSIST`、`PTTL` 覆盖命令执行、WATCH 版本推进、AOF 绝对过期时间回放和 TCP/redis-cli/redis-py smoke | `src/storage/engine.uya`、`src/persistence/aof.uya`、`src/command/router.uya`、`src/command/executor.uya`、`tests/unit/storage_engine_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| String 第一批增强命令可用：`APPEND`、`STRLEN`、`GETDEL`、`DELEX` 覆盖执行器、AOF replay、`DELEX IFEQ/IFNE` 条件删除、digest 条件未支持错误面、`COMMAND*` 可见面和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/command_introspection.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| String 第二批计数命令可用：`INCR`、`DECR`、`INCRBY`、`DECRBY` 覆盖执行器、错误路径和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| String 第三批原子写入命令可用：`GETSET`、`SETNX`、`SETEX` 覆盖执行器、过期语义、AOF 绝对过期回放和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`src/persistence/aof.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| String 第四批多 key 命令可用：`MGET`、`MSET`、`MSETNX` 覆盖 nullable array 回复、多 key 写入和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`src/persistence/aof.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| String 多 key TTL partial 可用：`MSETEX` 覆盖 `NX/XX` 条件、`EX/PX/EXAT/PXAT/KEEPTTL` 选项、动态 `COMMAND GETKEYS*`、条件失败 no-op AOF/backlog、成功写入的绝对过期 AOF replay 和 redis-cli/redis-py smoke | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`src/persistence/aof.uya`、`src/replication/backlog.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| String 第五批范围读写命令可用：`GETRANGE`、`SETRANGE` 覆盖负索引读取、空洞填充写入、AOF replay 和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`src/persistence/aof.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| String 第六批浮点计数命令可用：`INCRBYFLOAT` 覆盖浮点参数校验、结果归一化、NaN/Infinity 完整错误文本、AOF replay 和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Hash 第一批数值命令可用：`HINCRBY`、`HINCRBYFLOAT` 覆盖字段数值更新、浮点结果归一化、NaN/Infinity 完整错误文本和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Hash 第二批视图命令可用：`HKEYS`、`HVALS`、`HGETALL`、`HRANDFIELD` 覆盖字段/值枚举、键值对视图、deterministic random-field partial、`WITHVALUES` 与 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Hash 第三批扫描命令可用：`HSCAN` 覆盖 cursor 返回、`COUNT` 选项、空 key 行为与 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| List 第一批扩展命令可用：`RPUSH`、`RPOP`、`LINDEX`、`LSET`、`LLEN` 覆盖正负索引、尾部弹出、原地更新、`LSET` 缺失 key 完整错误文本与 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| List 第二批变异命令可用：`LINSERT`、`LTRIM`、`LREM` 覆盖 pivot 插入、区间裁剪、按次数删除与 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| List 第三批条件命令可用：`LPUSHX`、`RPUSHX`、`LPOS` 覆盖仅存在时写入、位置查找与 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Set 第一批随机取值命令可用：`SPOP`、`SRANDMEMBER` 覆盖空集合返回、成员返回与 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Set 第二批集合运算命令可用：`SINTER`、`SDIFF`、`SUNION` 覆盖有序结果回复、多 key 读取和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Set 第三批写回命令可用：`SINTERSTORE`、`SDIFFSTORE`、`SUNIONSTORE` 覆盖目标 key 写回、成员计数和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| ZSet 第一批数值与计数命令可用：`ZINCRBY`、`ZCARD`、`ZCOUNT` 覆盖整数 score 递增、成员计数和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| ZSet 第二批范围读取命令可用：`ZRANGE`、`ZLEXCOUNT`、`ZRANGEBYLEX`、`ZREVRANGE`、`ZREVRANGEBYLEX`、`ZRANGEBYSCORE`、`ZREVRANGEBYSCORE` 覆盖整数 score 区间读取、score range `WITHSCORES/LIMIT`、`ZRANGE BYSCORE/BYLEX`、`ZREVRANGE BYSCORE/BYLEX/LIMIT`、lex 开闭/无穷边界计数、lex `LIMIT` 正反向范围返回、升降序结果、`ZRANGE REV/WITHSCORES`、`ZREVRANGE WITHSCORES` 和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`src/storage/object.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| ZSet 第三批范围删除、扫描与读/写路径补齐命令可用：`ZREMRANGEBYLEX`、`ZREMRANGEBYRANK`、`ZREMRANGEBYSCORE`、`ZRANGESTORE`、`ZSCAN`、`ZRANDMEMBER`、`ZDIFF`、`ZDIFFSTORE`、`ZINTER`、`ZINTERCARD`、`ZINTERSTORE`、`ZUNION`、`ZUNIONSTORE` 覆盖按 lex 删除、按 rank 删除、按 score 删除、rank 范围写回、`ZRANGESTORE REV/BYSCORE/BYLEX/LIMIT`、scan 回复、deterministic random-member partial、差集读取、差集写回、交集读取、交集计数、交集写回、并集读取、并集写回、`WITHSCORES`、整数 `WEIGHTS`、`AGGREGATE SUM/MIN/MAX` 与 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key/Server 第三批命令可用：`RENAME`、`RENAMENX`、`LASTSAVE` 覆盖 key 改名、存在性语义、缺失 source 完整错误文本、最近保存时间与 redis-cli/redis-py/TCP smoke | `src/storage/engine.uya`、`src/command/router.uya`、`src/command/executor.uya`、`src/server.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key/Server 第四批清空命令可用：`FLUSHDB`、`FLUSHALL` 覆盖当前 DB 清空、watch 版本推进、AOF replay 与 redis-cli/redis-py/TCP smoke | `src/storage/engine.uya`、`src/command/router.uya`、`src/command/executor.uya`、`tests/unit/storage_engine_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key/Server 第五批绝对过期命令可用：`PEXPIREAT` 覆盖绝对过期时间设置、TTL 计算和 redis-cli/redis-py/TCP smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key/Server 第六批序列化命令可用：`DUMP`、`RESTORE` 覆盖项目内 RDB 子集 payload、相对毫秒 TTL 恢复、AOF replay 与 redis-cli/redis-py/TCP smoke | `src/storage/object.uya`、`src/command/router.uya`、`src/command/executor.uya`、`src/persistence/rdb.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key/Server 第七批单库与对象检查命令可用：`SELECT`、`OBJECT` 覆盖单库 `db0` 选择、对象编码/引用计数/空闲时间/FREQ 边界，以及 redis-cli/redis-py/TCP smoke | `src/storage/engine.uya`、`src/storage/object.uya`、`src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key/Server 第八批单库迁移/DB 管理命令可用：`MOVE` 在当前单库模式下覆盖同库错误与越界 DB 错误；`SWAPDB` partial 覆盖 `0 0` no-op、越界 DB 错误、非法整数和 `COMMAND*` 可见面，并通过 redis-cli/redis-py/TCP smoke 固化兼容边界 | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key/Server 第九批复制等待命令可用：`WAIT` 覆盖参数校验、负 timeout 错误和当前单机无副本场景返回 `0` 的兼容边界；`WAITAOF` partial 覆盖本地确认数组、当前无副本 AOF ACK 返回 `0`、参数校验、负 timeout 错误和 `COMMAND*` 可见面，并通过 redis-cli/redis-py/TCP smoke 固化 | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key/Server 第十五批诊断命令可用：`LOLWUT` partial 覆盖默认 bulk 文本、`VERSION` 整数校验、未知首参数默认输出、`COMMAND*` 可见面，并通过 redis-cli/redis-py/TCP smoke 固化当前不生成 Redis 原版动态图形的兼容边界 | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key/Server 第十批排序命令可用：`SORT` 覆盖 list/set/zset 源、`ASC/DESC`、`ALPHA`、`LIMIT`、`BY`、多 `GET`、`STORE`、hash pattern 和 `SORT STORE` 的 AOF replay | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Security baseline 可用：`requirepass`、`AUTH`、`SHUTDOWN` 覆盖配置解析、未认证 NOAUTH、密码校验、连接级认证状态、真实进程关闭和 redis-cli/redis-py/TCP smoke | `src/config.uya`、`src/main.uya`、`src/command/router.uya`、`src/network/connection.uya`、`src/server.uya`、`tests/unit/config_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |

## 15. `v0.9.1` 第一批

| DoD 项 | 证据 |
|--------|------|
| 官方命令全集矩阵可追踪：基于 Redis 8.6 命令页生成 `531` 个官方命令名、状态、目标版本与基础元数据，并落盘为命令矩阵文档 | `scripts/generate_command_catalog.py`、`docs/redis-uya-command-matrix.md` |
| 运行时共享目录可用：`src/command/catalog_generated*` 提供统一命令目录，`COMMAND` 家族与文档矩阵不再各维护一份命令名清单 | `src/command/catalog.uya`、`src/command/catalog_generated_base.uya`、`src/command/catalog_generated.uya`、`src/command/catalog_generated_part_*.uya` |
| `COMMAND` 控制面第一批可用：`COMMAND`、`COMMAND COUNT`、`COMMAND LIST`、`COMMAND INFO`、`COMMAND DOCS` 覆盖 RESP2/RESP3 基础返回、`LIST FILTERBY PATTERN/ACLCAT/MODULE`、`INFO` 未知命令占位和 `DOCS` 未知命令忽略边界 | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py` |
| `COMMAND DOCS` 无参数全量输出可用：覆盖 `531` 个目录项的 RESP2/RESP3 全量 docs 返回，以及服务端大响应发送第一批闭环 | `src/command/executor.uya`、`src/network/connection.uya`、`src/server.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py` |
| `COMMAND GETKEYS` / `COMMAND GETKEYSANDFLAGS` 当前命令表支持可用：覆盖 range key spec、多 key/成对 key、`RENAME` 双 key、`SORT ... STORE` / `BLMPOP` / `ZMPOP` / `BZMPOP` movablekeys、`RO/OW/RW/RM` 与 `access/update/insert/delete` 基础 flags 组合 | `src/command/executor.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py` |
| 单次读入批量执行可用：连接层会在一次读入中消费多个完整 RESP 顶层帧，覆盖 `MULTI/SET/GET/EXEC` 管线与 `redis-cli` stdin/pipeline 客户端路径 | `src/network/protocol.uya`、`src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/redis_cli_smoke.sh` |

## 16. `v0.9.1` 第二批

| DoD 项 | 证据 |
|--------|------|
| `CONFIG SET` 第二批运行时字段可用：支持 `port`、`bind`、`dir`、`dbfilename`、`appendfilename`、`requirepass`、`masterauth`、`replicaof`、`maxclients`、`databases`、`timeout`、`latency-tracking`、`latency-monitor-threshold`、`slowlog-log-slower-than`、`slowlog-max-len`，覆盖非法参数、`REPLICAOF NO ONE` 提升、运行时 `maxclients` 更新、timeout 回读和普通空闲连接关闭、latency histogram 采样开关、latency event 门限、slowlog 门限/保留长度和 `CONFIG GET` 回读 | `src/config.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`src/server.uya`、`tests/unit/config_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/idle_client.py` |
| `CONFIG REWRITE` 对应第二批核心字段落盘可用：运行时改写后的 `port`、`bind`、`dir`、`dbfilename`、`appendfilename`、`requirepass`、`masterauth`、`maxclients`、`databases`、`timeout`、`latency-tracking`、`latency-monitor-threshold`、`slowlog-log-slower-than`、`slowlog-max-len` 与内存策略可写回到重写配置文件 | `src/config.uya`、`src/command/executor.uya`、`src/server.uya`、`tests/unit/config_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/client_config_smoke.py` |
| String/Generic TTL 扩展可用：`GETEX`、`PSETEX`、`EXPIREAT`、`EXPIRETIME`、`PEXPIRETIME` 覆盖 key 提取 flags、绝对时间/相对时间 TTL 语义、AOF 绝对过期重放，以及 TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`src/persistence/aof.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Hash 字段基础扩展可用：`HDEL`、`HGETDEL`、`HEXISTS`、`HLEN`、`HMGET`、`HMSET`、`HSETNX`、`HSTRLEN` 覆盖 object helper、nullable array 回复、多 field legacy 写入、字段读取后删除、空 hash 删除与 AOF replay，以及 TCP/redis-py/redis-cli smoke | `src/storage/object.uya`、`src/command/router.uya`、`src/command/executor.uya`、`tests/unit/storage_object_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Hash field TTL 语义和持久化可用：`HGETEX`、`HSETEX`、`HEXPIRE`、`HPEXPIRE`、`HEXPIREAT`、`HPEXPIREAT`、`HTTL`、`HPTTL`、`HEXPIRETIME`、`HPEXPIRETIME`、`HPERSIST` 覆盖 `FIELDS numfields ...` 解析、field 级绝对毫秒过期元数据、`HGETEX` nullable bulk array 回复和 TTL 写入/清理、`HSETEX` field 写入与 `FNX/FXX/KEEPTTL`、`HEXPIRE/HPEXPIRE/HEXPIREAT/HPEXPIREAT` 的 `NX/XX/GT/LT` 条件、已到期时间删除、TTL option 语法和整数校验、整数数组回复、field/key 缺失 `-2` 或 Null Bulk、存在但无 field TTL `-1`、`HTTL/HPTTL/HEXPIRETIME/HPEXPIRETIME` 正 TTL/时间戳、`HPERSIST` 返回 `1`、普通 hash 访问 lazy expire、普通 hash 写入清理目标 field TTL、RDB save/load 与 DUMP/RESTORE bytes 保存 field TTL、AOF rewrite 通过 `HPEXPIREAT` 重建 field TTL、错类型/语法/整数错误、`COMMAND INFO/LIST/DOCS` 可见面和 TCP/redis-py/redis-cli smoke；当前不支持复制传播 field TTL | `src/storage/object.uya`、`src/command/router.uya`、`src/command/executor.uya`、`src/persistence/rdb.uya`、`src/persistence/rewrite.uya`、`src/command/catalog_generated_part_o.uya`、`src/command/catalog_generated_part_p.uya`、`src/command/catalog_generated_part_q.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/persistence_rdb_test.uya`、`tests/unit/persistence_rewrite_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Set 读路径扩展可用：`SCARD`、`SISMEMBER`、`SMISMEMBER`、`SSCAN` 覆盖成员计数、单成员命中、多成员顺序返回、scan cursor/COUNT 与 TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Set 交集计数扩展可用：`SINTERCARD` 覆盖 `numkeys`、`LIMIT`、空 key、错类型、参数错误边界与 TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Set 写路径补齐可用：`SMOVE` 覆盖同 key 搬移、空源返回、源/目标错类型、源清空删除、AOF replay 与 TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key 通用管理扩展可用：`TOUCH`、`UNLINK` 覆盖存在 key 计数、多 key 返回、删除后存在性边界与 TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| Key 模式读取扩展可用：`KEYS` 覆盖 `*` / `?` 最小 glob、字典序结果与 TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| 只读排序扩展可用：`SORT_RO` 覆盖只读排序返回、`STORE` 语法错误边界与 TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| 读路径补齐可用：`RANDOMKEY`、`TIME` 覆盖空库/单 key 语义、秒/微秒数组回复，以及 TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |
| 复制可观测读命令可用：`ROLE` 覆盖 master/replica 两种 RESP Array 返回形态，并通过 TCP/redis-py/redis-cli smoke 固化 | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh` |

## 17. `v0.9.1` 第三批

| DoD 项 | 证据 |
|--------|------|
| Pub/Sub pattern 第一批可用：`PSUBSCRIBE`、`PUNSUBSCRIBE`、`PUBLISH` 对 pattern 订阅推送 `pmessage`，并把直连/模式匹配接收者一起计入返回值 | `src/command/router.uya`、`src/network/connection.uya`、`tests/unit/command_router_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/pubsub_smoke.py` |
| Pub/Sub 管理面第一批可用：`PUBSUB HELP/CHANNELS/NUMPAT/NUMSUB` 复用连接订阅注册表返回当前频道与 pattern 计数，`PUBSUB SHARDCHANNELS/SHARDNUMSUB` 返回 `SSUBSCRIBE` shard 订阅真值 | `src/command/router.uya`、`src/network/connection.uya`、`tests/unit/command_router_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/pubsub_smoke.py` |
| `RESET` 连接上下文重置可用：覆盖 RESP3 -> RESP2 协议回退、订阅态退出、事务/观察键清理、tracking/client metadata 清空与 deauth 行为 | `src/command/router.uya`、`src/network/connection.uya`、`tests/unit/command_router_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/connection_reset_smoke.py` |
| Pub/Sub 订阅态命令限制第一批可用：RESP2 订阅态仅允许 `SUBSCRIBE/PSUBSCRIBE/SSUBSCRIBE/UNSUBSCRIBE/PUNSUBSCRIBE/SUNSUBSCRIBE/PING/QUIT/RESET`，RESP3 订阅态保持非 Pub/Sub 命令可用 | `src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/pubsub_smoke.py`、`tests/integration/connection_reset_smoke.py` |
| Pub/Sub 断开清理可用：连接关闭后会移除其频道/模式订阅项，后续 `PUBLISH` 不再把已断开连接计入接收者数量 | `src/server.uya`、`src/network/connection.uya`、`tests/integration/pubsub_smoke.py` |

## 18. `v0.9.2` 第一批

| DoD 项 | 证据 |
|--------|------|
| Blocking list 第一批可用：`BLPOP`、`BRPOP`、`BRPOPLPUSH`、`BLMOVE`、`BLMPOP` 覆盖立即命中、空源挂起、server-side unblock、超时返回、`COMMAND*` 可见性与 AOF replay | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`src/server.uya`、`src/persistence/aof.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/blocking_list_smoke.py`、`tests/integration/command_introspection.py`、`make test`、`make test-integration` |
| Blocking zset 第一批可用：`BZPOPMIN`、`BZPOPMAX`、`BZMPOP` 覆盖立即命中、空源挂起、server-side unblock、超时返回、`COMMAND*` 可见性与 AOF replay | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`src/server.uya`、`src/persistence/aof.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/blocking_zset_smoke.py`、`tests/integration/command_introspection.py`、`make test`、`make test-integration` |
| Sorted-set multi-pop partial 可用：`ZMPOP` 覆盖多 key 顺序探测、`MIN/MAX`、`COUNT`、空结果 Null Array、错类型/参数错误、`COMMAND*` 可见性、AOF replay 与 TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Bitmap 读路径第一批可用：`BITPOS` 覆盖 `0/1` 查找、缺省范围与 `start/end BYTE/BIT` 边界、missing key 特例、错类型/参数错误路径，以及 TCP/redis-py/redis-cli/`COMMAND*` smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test` |
| Bitmap 写路径第一批可用：`BITOP` 覆盖 `AND/OR/XOR/NOT`、all-missing 删除目标 key、`NOT` 单源限制及完整错误文本、目标 TTL 清理、AOF replay，以及 TCP/redis-py/redis-cli/`COMMAND*` smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Bitfield 第一批可用：`BITFIELD`、`BITFIELD_RO` 覆盖 `GET/SET/INCRBY`、`iN/uN` 编码、`#` 偏移、`OVERFLOW WRAP/SAT/FAIL`、只读约束、TTL 保留、AOF replay，以及 TCP/redis-py/redis-cli/`COMMAND*` smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| HyperLogLog 第一批 partial 可用：`PFADD`、`PFCOUNT`、`PFMERGE`、`PFSELFTEST` 与 `PFDEBUG` standalone-error 覆盖增量写入、单 key / 多 key 计数、merge、self-test `OK` 兼容面、内部调试命令安全错误面、AOF replay 与 TCP/redis-py/redis-cli/`COMMAND*` smoke；当前内部仍是 exact set-backed cardinality，而非 Redis 原生 HLL 编码 | `src/command/router.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_v.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Key copy partial 可用：`COPY source destination [DB 0] [REPLACE]` 覆盖当前单 DB 深拷贝、目标存在返回 `0`、`REPLACE` 覆盖、source TTL 保留、same-key 错误、非 `0` DB 错误、WATCH 版本推进、maxmemory noeviction、AOF replay、复制增量同步与 TCP/redis-py/redis-cli/`COMMAND*` smoke；当前不支持多 DB 目标复制 | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/replication_incremental_sync.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Restore asking partial 可用：`RESTORE-ASKING key ttl serialized-value [REPLACE] [ABSTTL] [IDLETIME seconds\|FREQ frequency]` 复用 `RESTORE` 的单 DB RDB payload 写入路径，覆盖路由、执行器、`COMMAND INFO/LIST/DOCS/GETKEYS` 可见面和 redis-py/redis-cli smoke；支持 `REPLACE` 覆盖已有 target 并按新 TTL 写入，支持 `ABSTTL` 绝对毫秒过期时间，支持 `IDLETIME`/`FREQ` 恢复 LRU/LFU 元数据，当前不校验集群 ASKING 状态 | `src/command/router.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_x.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Geo 第一批 partial 可用：`GEOADD`、`GEODIST`、`GEOHASH`、`GEOPOS`、`GEOSEARCH`、`GEOSEARCHSTORE`、`GEORADIUS`、`GEORADIUS_RO`、`GEORADIUSBYMEMBER`、`GEORADIUSBYMEMBER_RO` 覆盖基础写入、距离查询、geohash 查询、坐标查询、search store 写回、legacy radius 查询与写回、missing member/null 返回及完整 center-member 错误文本、`FROMMEMBER/FROMLONLAT`、`BYRADIUS/BYBOX`、`ASC/DESC`、`COUNT`、`WITHDIST/WITHCOORD/WITHHASH`、`STORE`、`STOREDIST` 整数距离 score、AOF replay 与 TCP/redis-py/redis-cli/`COMMAND*` smoke；当前内部仍是 exact zset-backed packed coordinate score，而非 Redis 原生 geohash 编码，`GEOSEARCHSTORE` 和写型 legacy radius 的 `STOREDIST` 暂不保存 Redis 原生浮点距离 | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Scripting 第一批 partial 可用：`EVAL`、`EVALSHA`、`EVAL_RO`、`EVALSHA_RO`、`SCRIPT DEBUG/LOAD/EXISTS/FLUSH/KILL` 覆盖脚本缓存、SHA1 查找、只读脚本拒写、`SORT ... STORE` 参数驱动写路径拒绝、完整 `SCRIPT HELP` 数组、完整 numkeys/内部命令/`SCRIPT FLUSH` 参数错误文本、事务执行、AOF / 复制传播实际命令效果、debug 模式 no-op、无运行脚本 `KILL` 错误面，以及 TCP/redis-py/redis-cli/`COMMAND*` smoke；当前只支持单条 `return redis.call(...)` 脚本子集，不支持多语句 Lua、`redis.pcall` 或 Redis Lua debugger，`SCRIPT KILL` 仅覆盖 no-running-script 兼容面 | `src/command/router.uya`、`src/network/connection.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/replication_incremental_sync.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Functions 最小 library 平面可用：`FUNCTION LOAD` 接受一个 `#!lua name=<library>` 和最多 8 个位置参数或 table 形式 `redis.register_function(... return redis.call(...) end)` 注册子集并返回库名；table 注册支持最多 128 字节 description 和 `flags = {'no-writes'}`，`LIST [LIBRARYNAME <pattern>] [WITHCODE]` 支持 `*` / `?` glob 并回显 `name/description/flags`，`STATS`、`DELETE`、`FLUSH` 反映真实进程内状态；`FCALL`/`FCALL_RO` 按函数名执行该子集，`no-writes` 与 `FCALL_RO` 共用内部写命令拒绝，并复用脚本的 ACL 与实际数据命令 AOF/复制传播路径。其他 flags、函数库元数据 RDB/AOF/复制和非空 `DUMP/RESTORE` 仍未支持 | `src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Functions 第一批 partial 可用：`FUNCTION HELP`、`FUNCTION LIST`、`FUNCTION STATS`、`FUNCTION FLUSH`、`FUNCTION DELETE`、`FUNCTION LOAD`、`FUNCTION DUMP`、`FUNCTION RESTORE`、`FUNCTION KILL`、`FCALL`、`FCALL_RO` 覆盖路由、执行器帮助数组含 `DELETE`、空库列表、空库统计、no-op flush、空库删除错误面、加载未支持错误面、空库序列化 payload、空库 payload restore、无运行脚本错误面、空库调用错误面、参数校验、`COMMAND GETKEYS*`、`COMMAND INFO/LIST/DOCS` 可见面、TCP/redis-py/redis-cli smoke；当前不支持 function library 存储、非空 `FUNCTION RESTORE` 或真实 `FCALL/FCALL_RO` 执行 | `src/command/router.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_m.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| ACL 第一批 partial 可用：`ACL CAT`、`ACL DELUSER`、`ACL DRYRUN`、`ACL GENPASS`、`ACL GETUSER`、`ACL HELP`、`ACL LIST`、`ACL LOAD`、`ACL LOG`、`ACL SAVE`、`ACL SETUSER`、`ACL USERS`、`ACL WHOAMI` 覆盖路由、执行器 ACL 分类、默认用户不可删除错误面、默认用户 dry-run 命令检查、口令生成、默认用户详情、帮助数组、默认用户 config 格式、`requirepass` 哈希标记回显、ACL 文件未配置错误面、默认用户列表、named user 进程内元数据创建/列出/详情/删除/dry-run 用户存在性检查、口令写入、AUTH/HELLO AUTH 认证、当前用户名回显、默认用户和 named user 命令级 `+cmd/-cmd` 与分类级 `+@category/-@category` deny list、`resetcommands` 恢复命令规则、`allkeys/resetkeys/~pattern` 固定 key range 权限 partial、`allchannels/resetchannels/&pattern` Pub/Sub channel 权限 partial、`clearselectors/resetselectors` 兼容 no-op、`NOPERM` 真实连接拒绝路径、事务队列前置拒绝、脚本内部命令拒绝、命令/key/channel 权限拒绝 `ACL LOG [count]` 基础审计字段与真实 client id/addr/laddr、`ACL LOG RESET`、参数错误、未知子命令错误、`COMMAND INFO/LIST/DOCS` 可见面、redis-py/redis-cli/`COMMAND*` smoke；当前支持 ACL named user 进程内元数据、口令写入与认证最小闭环，不支持 selector 权限、ACL 文件加载保存或 Redis 动态 key spec / movablekeys 完整 key 解析，安全基线仍由 `requirepass` / `AUTH` 提供 | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`src/command/catalog_generated_part_a.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Client reply partial 可用：`CLIENT REPLY ON|OFF|SKIP` 覆盖连接级回复抑制状态、单次 skip、`OFF` 期间命令继续执行、`RESET` 清理、`CLIENT HELP` 精确条目、`COMMAND INFO/LIST/DOCS` 可见面和真实 TCP/redis-cli smoke；当前只覆盖命令回复抑制，不改变 Pub/Sub push 或 `MONITOR` 推送 | `src/network/connection.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_f.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/command_introspection.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Client info/list observation partial 可用：`CLIENT INFO` 与 `CLIENT LIST` 复用信息行编码，覆盖真实连接 `id/addr/laddr/fd/age/idle/db/user/flags/cmd/redir/events/io-thread/multi/watch/sub/psub/ssub/qbuf/qbuf-free/argv-mem/multi-mem/rbs/rbp/obl/oll/omem/tot-mem`、客户端名、RESP 版本、事务/Pub/Sub 状态和 library 元数据；`age/idle` 覆盖 accept、最近读入、当前连接 idle 归零和静置对端秒级增长，`cmd` 覆盖 `client|info`、`client|list`、`config|get`、`multi/discard`、`subscribe/psubscribe/ssubscribe`、`monitor`、阻塞 `blpop` 和小写规范化，`redir` 覆盖默认 `-1`、tracking redirect 目标及 `TRACKING OFF` 清理，`multi` 覆盖事务外 `-1`、空事务 `0`、排队命令计数和 `DISCARD` 清理，Redis 7.4+ `watch` 覆盖观察 key 去重数量与 `UNWATCH/EXEC` 清零，`multi-mem` 覆盖事务外/空事务零值、单条 14 字节 RESP 命令排队后的实际队列负载和 `DISCARD` 清零，并明确不伪造 Redis argv 指针开销；`sub/psub/ssub` 覆盖默认零值及单连接普通、pattern、shard 混合订阅的独立真值；`qbuf/qbuf-free` 覆盖当前 INFO 请求的完整已读长度和已消费完对端的 `0/8192` 固定缓冲真值；`argv-mem=0` 覆盖普通与空闲 Pub/Sub 连接，来自参数借用输入缓冲且连接状态不持有 argv 堆对象的所有权真值；`rbs/rbp` 覆盖固定响应区容量和初始化峰值 `81920/81920`，共享编码器直接输出且不增加发送热路径状态；`obl` 覆盖普通/零拷贝异步发送的剩余逻辑字节和完成清零，慢读测试以交替 `GET/MGET` 命令握手稳定触发背压并从另一连接观察非零值，当前无动态回复链表所以 `oll/omem=0`；`tot-mem` 覆盖 server 固定客户端槽位与输出分配、空事务固定值、排队后加入负载和对象元数据、`DISCARD` 精确恢复以及共享 Pub/Sub 注册表不重复分摊，并按 allocator 请求字节递归计入事务/WATCH/tracking 独占对象；`events` 覆盖普通与空闲 Pub/Sub 的 `r` 及慢读背压的 `w`，直接由 `obl` 派生；Redis 8.0 `io-thread=0` 覆盖当前单事件循环线程编号且不增加连接状态；列表通过活跃事务地址读取 `cmd/redir/multi/watch/multi-mem/tot-mem/qbuf/obl/events` 并仅在序列化冷路径扫描事务队列、独占动态对象和订阅注册表；`db=0` 由固定单数据库配置和 `SELECT` 约束保证，`user` 覆盖默认用户、named-user `AUTH` 跨连接即时可见和认证状态同步；`flags` 覆盖普通 `N`、MONITOR `O`、Pub/Sub `P`、事务 `x`、阻塞 `b`、tracking `t`、tracking broadcast `B`、WATCH dirty-CAS `d`、NO-EVICT `e`、NO-TOUCH `T` 及组合状态；`d` 覆盖跨连接写入后的 INFO/LIST 真值和 `UNWATCH/EXEC` 清理，并仅在诊断冷路径比较 WATCH 版本；`CLIENT LIST ID id...` 覆盖多 ID 顺序、重复项、缺失项与非法 ID，`CLIENT LIST TYPE NORMAL\|MASTER\|REPLICA\|PUBSUB` 覆盖真实 NORMAL/PUBSUB 分类、standalone 复制类型空结果、`SLAVE` 兼容别名、动态未知类型错误、TYPE/ID 互斥、帮助可见面、单元测试、真实 TCP/redis-cli smoke 和父提交 A/B 性能检查；当前复制链路客户端尚未接入连接注册表 | `src/network/connection.uya`、`src/server.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/idle_client.py`、`tests/integration/slow_reader.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Client kill partial 可用：`CLIENT KILL ip:port` 覆盖连接注册表远端地址精确关闭、成功 `OK`、缺失地址 `ERR No such client`；现代 `CLIENT KILL [ID id] [ADDR ip:port] [SKIPME yes\|no]` 覆盖 ID/ADDR 单目标交集、重复 selector、缺失/不相交返回 `0`、默认跳过当前连接、`SKIPME no` 关闭当前连接、帮助与真实 TCP/redis-cli smoke；当前仍不支持 `LADDR/TYPE/USER/MAXAGE` 多条件、多目标 filters | `src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Client unblock partial 可用：`CLIENT UNBLOCK id [TIMEOUT\|ERROR]` 覆盖阻塞 pop 等待客户端的 timeout/error 解除、未阻塞目标返回 `0`、参数错误、`CLIENT HELP`、`COMMAND INFO/LIST/DOCS` 可见面和真实 TCP/redis-cli smoke；当前只覆盖 redis-uya 内置阻塞 pop 等待，不支持模块阻塞客户端类型 | `src/network/connection.uya`、`src/server.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_f.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/command_introspection.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Client pause write partial 可用：`CLIENT PAUSE timeout WRITE` 覆盖控制连接豁免、读命令继续执行、写命令等待 `CLIENT UNPAUSE` 后恢复、`ALL` 继续阻塞全部命令、非法 timeout 完整错误文本和真实 TCP smoke；当前基于命令目录写标志与已解析 batch 判断，不实现 Redis 原生跨线程 pause 协调 | `src/server.uya`、`src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`make test`、`make test-integration` |
| Client caching partial 可用：`CLIENT CACHING YES|NO` 覆盖连接级标志存储、参数错误、`RESET` 清理、`CLIENT HELP`、`COMMAND INFO/LIST/DOCS` 可见面和真实 TCP/redis-cli smoke；当前只保存兼容标志，尚未提供 server-assisted client-side caching invalidation | `src/network/connection.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_f.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/command_introspection.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Client tracking prefix partial 可用：`CLIENT TRACKING ON BCAST PREFIX prefix ...` 覆盖连接级 prefix 列表存储、`PREFIX` without `BCAST` 错误、`TRACKINGINFO` 回读、`CLIENT INFO/LIST flags=tB`、`CLIENT HELP` 可见面和真实 TCP smoke；当前只保存兼容状态，不发送 invalidation push | `src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`make test`、`make test-integration` |
| Client no-evict partial 可用：`CLIENT NO-EVICT ON|OFF` 覆盖连接级标志存储、`CLIENT INFO/LIST flags=e`、参数错误、`RESET` 清理、`CLIENT HELP`、`COMMAND INFO/LIST/DOCS` 可见面和真实 TCP/redis-cli smoke；当前只保存兼容标志，尚未接入 `maxmemory` 淘汰候选保护 | `src/network/connection.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_f.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/command_introspection.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Client no-touch partial 可用：`CLIENT NO-TOUCH ON|OFF` 覆盖连接级标志存储、`CLIENT INFO/LIST flags=T`、参数错误、`RESET` 清理、`CLIENT HELP`、`COMMAND INFO/LIST/DOCS` 可见面、真实 TCP/redis-cli smoke，以及命令执行/阻塞 pop 预检查对象访问路径的 LRU/LFU touch 抑制 | `src/network/connection.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_f.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/command_introspection.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Module 第一批 partial 可用：`MODULE HELP`、`MODULE LIST` 覆盖路由、执行器帮助数组、空模块列表、容器 arity 完整错误文本、参数错误、未知子命令错误、`COMMAND INFO/LIST/DOCS` 可见面、TCP/redis-py/redis-cli/`COMMAND*` smoke；`MODULE LOAD/LOADEX/UNLOAD` 当前为单机安全 profile 的 `standalone-error`，进入 `COMMAND*` 可见面但不加载/卸载动态模块、不修改模块列表、不写 AOF 或复制 backlog | `src/command/router.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_u.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| String LCS partial 可用：`LCS key1 key2`、`LCS key1 key2 LEN` 与 `LCS key1 key2 IDX [MINMATCHLEN n] [WITHMATCHLEN]` 覆盖基础最长公共子序列、长度返回、匹配段索引明细、缺失 key 空串语义、错类型、`COMMAND INFO/LIST/DOCS/GETKEYS/GETKEYSANDFLAGS` 可见面和 redis-py/redis-cli smoke；当前仍不支持大于 4096 字节输入的无限制 DP 计算 | `src/command/router.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_s.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| String SUBSTR alias 可执行：`SUBSTR key start end` 复用 `GETRANGE` 范围读取语义，覆盖路由、执行器、catalog alias 状态、`COMMAND INFO/LIST/DOCS/GETKEYS/GETKEYSANDFLAGS`、TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| String INCREX partial 可用：`INCREX key [BYINT n \| BYFLOAT n] [LBOUND min] [UBOUND max] [SATURATE] [EX/PX/EXAT/PXAT/PERSIST] [ENX]` 覆盖整数递增、浮点递增、上下界 no-op、饱和钳制、NaN/Infinity 完整错误文本、TTL 设置/保留/清理、错类型、`COMMAND INFO/LIST/DOCS/GETKEYS/GETKEYSANDFLAGS` 可见面和 TCP/redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`src/command/catalog_generated_part_q.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| String DIGEST partial 可用：`DIGEST key` 覆盖 128 字节以内 String 的 XXH3_64 十六进制 digest、missing Null Bulk、错类型、超长输入显式 partial 错误、`DELEX IFDEQ/IFDNE` 短字符串 digest 条件删除、`COMMAND INFO/LIST/DOCS/GETKEYS/GETKEYSANDFLAGS` 可见面和 redis-py/redis-cli smoke | `src/command/router.uya`、`src/command/executor.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Memory 第一批 partial 可用：`MEMORY HELP`、`MEMORY STATS`、`MEMORY USAGE`、`MEMORY DOCTOR`、`MEMORY MALLOC-STATS`、`MEMORY PURGE` 覆盖 allocator / object-pool / `maxmemory` 观测、近似 key 占用、诊断文本、allocator bulk report、no-op purge、容器 arity 完整错误文本、TCP/redis-py/redis-cli/`COMMAND*` smoke；`USAGE` 返回 redis-uya 运行时近似占用，`MALLOC-STATS` 返回 redis-uya allocator / object-pool 计数，均非 Redis 原生 jemalloc 口径，`PURGE` 不触发 Redis jemalloc purge 级别的 allocator 行为 | `src/command/router.uya`、`src/command/executor.uya`、`src/storage/dict.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Slowlog 第一批 partial 可用：`SLOWLOG HELP`、`SLOWLOG LEN`、`SLOWLOG GET`、`SLOWLOG RESET` 覆盖空日志、固定容量 ring 读取、重置、事务内执行、容器 arity 完整错误文本、`CONFIG SET slowlog-log-slower-than` 门限控制、`CONFIG SET slowlog-max-len` 保留长度裁剪、TCP/redis-py/redis-cli/`COMMAND*` smoke；当前 slowlog 仅保留 redis-uya 进程内最近命令，`duration_us` 由运行时时间源在命令执行前后采样但精度受毫秒级时钟限制，客户端地址固定为占位值，内部最多保留 `128` 条，尚不支持原生客户端端点真值 | `src/config.uya`、`src/command/router.uya`、`src/command/executor.uya`、`src/command/slowlog.uya`、`tests/unit/config_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/client_config_smoke.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Latency 第一批 partial 可用：`LATENCY HELP`、`LATENCY LATEST`、`LATENCY HISTORY`、`LATENCY RESET`、`LATENCY DOCTOR`、`LATENCY HISTOGRAM`、`LATENCY GRAPH` 覆盖空事件返回、`command` 事件历史、top-level 命令累计直方图、容器子命令累计直方图、`CONFIG RESETSTAT` 清理直方图、`CONFIG SET latency-tracking yes\|no` 采样开关、`CONFIG SET latency-monitor-threshold` 事件门限、诊断文本、参数错误、TCP/redis-py/redis-cli/`COMMAND*` smoke；当前采样普通命令执行耗时并保留进程内 `command` 事件历史，普通命令按 top-level 名称聚合，容器命令按 `parent\|subcommand` 名称聚合，耗时精度受毫秒级时间源限制 | `src/config.uya`、`src/command/router.uya`、`src/command/executor.uya`、`src/command/latency.uya`、`src/command/catalog_generated_part_s.uya`、`tests/unit/config_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`tests/integration/client_config_smoke.py`、`make test`、`make test-integration` |
| Monitor 第一批 partial 可用：`MONITOR` 覆盖进入流式观测模式、跨连接命令推送、`RESET` 退出、连接关闭清理、`COMMAND*` 可见性与真实 TCP smoke；全局活动订阅计数与 fd 槽表同步，无订阅者时普通命令跳过监控行分配，`RESET`、连接/事务关闭和发送失败会清理计数。当前监控行使用 redis-uya 占位端点，不包含 Redis 原生客户端地址、DB 切换真值或微秒精度时间 | `src/command/router.uya`、`src/network/connection.uya`、`src/server.uya`、`src/command/catalog_generated_part_u.uya`、`tests/unit/command_router_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/monitor_smoke.py`、`tests/integration/command_introspection.py`、`make test`、`make test-integration` |
| Debug standalone-error 可用：`DEBUG subcommand [arg ...]` 进入运行时路由、命令矩阵和 `COMMAND INFO/LIST/DOCS` 可见面，但在 redis-uya 单机安全 profile 中统一返回禁用错误，不开放 Redis 内部调试/破坏性子命令，也不写 AOF/复制 backlog | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`src/command/catalog_generated_part_j.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Failover standalone-error 可用：`FAILOVER [TO host port [FORCE]] [ABORT] [TIMEOUT milliseconds]` 进入运行时路由、命令矩阵和 `COMMAND INFO/LIST/DOCS` 可见面；当前无副本/未实现 controlled failover 状态机时返回 Redis 兼容的 connected-replica 错误，不提升 replica、不切换复制角色、不写 AOF/复制 backlog | `src/command/router.uya`、`src/command/executor.uya`、`src/network/connection.uya`、`src/command/catalog_generated_part_k.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |
| Module 加载/卸载安全错误面可用：`MODULE LOAD`、`MODULE LOADEX` 与 `MODULE UNLOAD` 进入命令矩阵和 `COMMAND INFO/LIST/DOCS`，但在 redis-uya 单机安全 profile 下统一返回禁用错误，不加载动态库、不修改模块列表、不写 AOF 或复制 backlog | `src/command/executor.uya`、`scripts/generate_command_catalog.py`、`src/command/catalog_generated_part_u.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/command_introspection.py`、`tests/integration/redis_py_subset.py`、`tests/integration/smoke_tcp.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration` |

## 19. `v0.9.3` 第一批

| DoD 项 | 证据 |
|--------|------|
| Streams 第一批 partial 可用：`XADD`、`XLEN`、`XRANGE`、`XREVRANGE`、`XREAD` 覆盖基础追加、自动/显式 id、`NOMKSTREAM` 缺失 key 空返回、`MAXLEN|MINID [=|~] threshold [LIMIT count]` 追加后头部裁剪、长度、正反向范围、`COUNT`、非阻塞读取、错类型、RDB save/load、AOF rewrite 与真实 TCP smoke；当前不支持 `XREAD BLOCK`、consumer group 或 Redis 原生 radix-tree/listpack 编码，普通 AOF append 对 `XADD *` 回放会重新生成 ID | `src/storage/object.uya`、`src/command/router.uya`、`src/command/executor.uya`、`src/persistence/rdb.uya`、`src/persistence/rewrite.uya`、`tests/unit/storage_object_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/persistence_rdb_test.uya`、`tests/unit/persistence_rewrite_test.uya`、`tests/integration/streams_smoke.py`、`tests/integration/command_introspection.py`、`make test`、`make test-integration` |
| Streams 第二批 partial 可用：`XTRIM MAXLEN|MINID [=|~] threshold [LIMIT count]` 覆盖 stream 头部裁剪、按 ID 头部裁剪、删除数量上限、空 key 返回 `0`、错类型、`COMMAND*` 可见性与真实 TCP/AOF replay smoke；`XDEL key id [id ...]` 覆盖精确 ID 删除、缺失 ID/缺失 key 返回 `0`、错类型、`COMMAND*` 可见性与真实 TCP/AOF replay smoke；`XCFGSET key [IDMP-DURATION seconds] [IDMP-MAXSIZE size]` 覆盖参数解析、缺失 key、错类型、`COMMAND*` 可见性与真实 TCP smoke；`XIDMPRECORD key pid iid stream-id` 覆盖 pid/iid、stream ID、entry 存在性、缺失 key、错类型、`COMMAND*` 可见性与真实 TCP smoke；`XDELEX key [KEEPREF\|DELREF\|ACKED] IDS numids id [id ...]` 覆盖 per-id 删除状态、缺失 key 返回 `-1`、错类型、坏 ID、`IDS` 数量校验、`COMMAND*` 可见性与真实 TCP/AOF replay smoke；`XACK key group id [id ...]`、`XACKDEL key group [KEEPREF\|DELREF\|ACKED] IDS numids id [id ...]`、`XNACK key group SILENT\|FAIL\|FATAL IDS numids id [id ...] [RETRYCOUNT n] [FORCE]`、`XAUTOCLAIM key group consumer min-idle-time start [COUNT count] [JUSTID]`、`XCLAIM key group consumer min-idle-time id [id ...]`、`XREADGROUP GROUP group consumer [COUNT count] [NOACK] STREAMS key [key ...] id [id ...]` 与 `XPENDING key group [IDLE min-idle-time] start end count [consumer]` 覆盖无 group 时的 `NOGROUP`、缺失 key、错类型、`COMMAND*` 可见性与真实 TCP smoke；`XGROUP HELP` / `XINFO HELP` 覆盖帮助兼容面、错误参数和 `COMMAND*` 可见性；`XGROUP CREATE key group id [MKSTREAM]` 覆盖参数解析、缺失 key、错类型、明确未支持错误、`COMMAND*` 可见性与真实 TCP smoke；`XGROUP CREATECONSUMER key group consumer` 与 `XGROUP DELCONSUMER key group consumer` 覆盖无 group 时的 `NOGROUP`、缺失 key、错类型、`COMMAND*` 可见性与真实 TCP smoke；`XGROUP DESTROY key group` 覆盖 empty-state 返回 `0`、缺失 key、错类型、`COMMAND*` 可见性与真实 TCP smoke；`XGROUP SETID key group id [ENTRIESREAD n]` 覆盖无 group 时的 `NOGROUP`、缺失 key、错类型、`COMMAND*` 可见性与真实 TCP smoke；`XSETID key last-id [ENTRIESADDED n] [MAXDELETEDID id]` 覆盖参数解析、缺失 key、错类型、`last-generated-id` 写入、倒退 ID 拒绝、`COMMAND*` 可见性与真实 TCP smoke；`XINFO STREAM key [FULL [COUNT count]]` 覆盖 key-only 基础元数据、FULL entry 明细、缺失 key、错类型、`COMMAND*` 可见性与真实 TCP smoke；`XINFO GROUPS key` 覆盖 empty-state 空数组、缺失 key、错类型、`COMMAND*` 可见性与真实 TCP smoke；`XINFO CONSUMERS key group` 覆盖无 group 时的 `NOGROUP`、缺失 key、错类型、`COMMAND*` 可见性与真实 TCP smoke；非法 stream ID、倒退 ID、缺失 key 和 `XGROUP CREATE` arity 均有完整错误文本单元/TCP 断言；当前 `~` 仅作为语法兼容占位，仍按精确裁剪执行，`XACKDEL` / `XCFGSET` / `XIDMPRECORD` / `XNACK` / `XAUTOCLAIM` / `XCLAIM` / `XDEL` / `XDELEX` / `XREADGROUP` 不维护 consumer group PEL 或 IDMP 元数据，`XSETID` 不保存 `ENTRIESADDED` / `MAXDELETEDID` 独立元数据，不支持真实 consumer group 状态命令 | `src/storage/object.uya`、`src/command/router.uya`、`src/command/executor.uya`、`tests/unit/storage_object_test.uya`、`tests/unit/command_router_test.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/streams_smoke.py`、`tests/integration/command_introspection.py`、`make test`、`make test-integration` |
| 复制 backlog 摊销裁剪可用：固定容量满后先前移逻辑起点，累计废弃前缀达到阈值后再物理压缩，保持 `histlen`、first offset 与 PSYNC slice 语义不变；release 1000 次短测中 `SET 1KiB` 从 1468 提升到 4078 req/s、p99 从 977us 降到 442us，5000 次长测达到 2041 req/s | `src/replication/backlog.uya`、`tests/unit/replication_backlog_test.uya`、`make test`、`tests/integration/replication_psync_backlog.py`、`tests/integration/replication_full_sync.py`、`tests/integration/replication_incremental_sync.py`、`tests/integration/replication_consistency.py`、`benchmarks/v0.9.3-performance-analysis.md` |
| Release 构建与 benchmark 口径可复现：`make build-release` 使用 Uya `-O1` 与宿主 C `-O3 -DNDEBUG`，构建模式写入 sidecar 并由 benchmark 记录；当前正式矩阵使用 50000 次请求和 2000 次预热。零拷贝 writev 首次直写后的不可变与 current 报告五项绝对吞吐、normalized 和 p99 guard 全部通过；不可变五项 Redis 吞吐比为 `1.01x/1.23x/0.99x/1.24x/1.17x`，current 为 `0.95x/1.24x/0.99x/1.26x/1.14x`。GET 1KiB 达到本轮 1.10x 吞吐且 p99 优于 Redis，PING 与 GET 16B 仍未达到，不能宣称读路径全面超越 | `Makefile`、`scripts/benchmark_v0_8_0.py`、`docs/redis-uya-benchmark-format.md`、`benchmarks/v0.9.3-release-performance-2026-08-02-writev-direct-50k.md`、`benchmarks/v0.9.3-release-performance.md` |
| 固定空回复直接构造可用：`connection_empty_reply` 保持原 `CommandReply` 全字段初值，但不再调用通用构造器生成第二个 200 字节临时对象；release 删除嵌套构造调用和专用 constprop body，文本段缩小 208 字节。四组反向固定 CPU 200K `GET 1KiB` 的 instrumented throughput 提升 0.56%，cycles/instructions/branches 分别下降 0.71%/0.20%/0.11%，完整单测、33 项集成与正式 50K guard 通过 | `src/network/connection.uya`、`make test`、`make test-integration`、`benchmarks/v0.9.3-release-performance-2026-07-27-empty-reply-direct-50k.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| RESP 连续写入容量预检可用：`connection_append_bytes`、`connection_append_crlf` 和非零 `connection_append_u64` 在写入前验证完整剩余容量，成功后直接写入并一次更新位置；错误仍返回 `ConnectionOutputTooSmall`，成功 RESP 字节保持不变。单测覆盖 19 位最大 i64 的 22 字节精确缓冲和 21 字节失败边界，完整单测与 33 项集成通过。两组反向 100K 墙钟吞吐提升 1.10%；四组反向固定 CPU 200K `GET 1KiB` 的 instrumented throughput 提升 0.73%，cycles/instructions/branches/branch-misses 分别下降 0.99%/0.34%/0.51%/1.05%，p99 下降 0.39%。release 文本段增加 400 字节，零丢样复采样中原 0.36% `connection_append_byte` 热点消失；不可变与复跑 current 50K 的绝对吞吐、p99 和总体 guard 通过 | `src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`make test-redis-cli`、`bash scripts/verify_definition_of_done.sh`、`benchmarks/v0.9.3-release-performance-2026-07-27-append-preflight-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 零拷贝结果包装层删除可用：server 直接调用已有 `zero_copy_at` 入口，成功解包 `ConnectionProcessResult` 后同步 `close_after_write`，删除唯一调用且逐字段重建完整结果的 `zero_copy_close_at` wrapper；QUIT、半包和错误关闭语义保持不变。release 删除 377 字节 wrapper，同时物化 273 字节直接导出入口，连同调用点变化后文本段净减 72 字节。100K 墙钟 A/B 基本持平；两组相反顺序固定 CPU 200K 的 8 次服务端计数合并后 instructions/branches/branch-misses 分别下降 0.251%/0.231%/2.618%，cycles 受频率波动上升 0.520%。源码契约、完整单元、33 项集成及不可变/current 50K 绝对吞吐、p99 和总体 guard 通过 | `src/network/connection.uya`、`src/server.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`benchmarks/v0.9.3-release-performance-2026-07-31-zero-copy-wrapper-elision-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 空 expires 惰性检查快速路径可用：`key_is_expired` 在 expires 主表与 rehash 表的 `used` 均为零时直接返回未过期，不再为无 TTL GET 先计算一次完整 key CRC64；存在任一 TTL、主动 rehash 或真实过期项时继续走原 `dict_lookup`。从未分配 expires 表的普通 set/get 与分配后由 `clear_key_expire` 清空的表均由现有 engine 单测覆盖。通用 `dict_lookup` 空表检查的试验方案墙钟回归 8.2% 后已丢弃；最终定向方案两组反向固定 CPU 100K `GET 1KiB` 墙钟吞吐提升 7.80%、p99 下降 12.35%，两组反向 200K `perf stat` 吞吐提升 2.52%，cycles/instructions/branches/branch-misses 分别下降 9.47%/4.63%/2.26%/5.72%。源码契约、完整单元、33 项集成以及不可变/current 50K 绝对吞吐、p99 和总体 guard 通过 | `src/storage/engine.uya`、`tests/unit/storage_engine_test.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`benchmarks/v0.9.3-release-performance-2026-07-31-empty-expire-fast-path-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| GET 字符串结果直达可用：命中对象后直接检查 `obj_type == object_string` 并把 `obj.value.buf[0:obj.value.len]` 交给 bulk reply，不再调用返回错误联合切片的 `command_executor_string_value`；缺失 key 仍返回 Null Bulk，非字符串仍返回 WRONGTYPE，通用 helper 保留给其他命令。release 反汇编确认 GET 无目标调用、少保存一个寄存器、栈帧减少 8 字节，函数从 702 字节降到 691 字节。四次相反顺序 100K 墙钟为 -0.29%、p99 改善 2.92%；两组相反顺序 200K `perf stat` 共 8 次测量中吞吐提升 1.04%，cycles/instructions/branches 分别下降 0.66%/1.02%/1.02%。零丢样 10K profile 达到 14305 req/s，目标 helper 无样本；源码契约、完整单元、33 项集成及不可变/current 50K 全部 guard 通过 | `src/command/executor.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`benchmarks/v0.9.3-release-performance-2026-07-31-get-string-direct-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| GET AOF 判定门控可用：普通顶层 `GET` 在 AOF writer 存在时按 `CommandType.command_get` 直接跳过 `connection_should_append_aof`；脚本、事务、全部写命令及参数驱动写入的 `SORT ... STORE` 保持原判定路径。AOF 单测断言 GET 前后 `writer.buffer_len` 不变，并把 RPUSH + `SORT STORE` 改为经真实连接层追加后回放。release 标准命令函数仅增加 15 字节，完整二进制仍为 1597232 字节；8 轮反向固定 CPU 200K 墙钟吞吐提升 2.48%、p99 下降 7.63%，`perf stat` 固定请求 instructions/branches 下降 0.64%/0.68%，cycles 受频率波动上升 3.07%。零丢样 11K profile 达到 13476 req/s，目标 helper 无样本；源码契约、完整单元、33 项集成及不可变/current 50K 三类 guard 全部通过 | `src/network/connection.uya`、`tests/unit/persistence_aof_test.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`benchmarks/v0.9.3-release-performance-2026-07-31-get-aof-gate-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 零拷贝 writev 首次直写可用：大 bulk GET 先在 client coroutine 中调用共享 `server_writev_progress_nonblocking`，完整首写不再构造/轮询 writev Future；只有部分写或 EAGAIN 才把精确 head/body/tail offset 交给原 writable continuation。4 轮反向 100K 墙钟吞吐提升 2.54%、p99 下降 8.63%；8 轮反向固定 CPU 200K `perf stat` 吞吐提升 1.51%，cycles/instructions/branches/branch-misses 下降 1.25%/2.94%/3.62%/1.38%。release Future poll 从 798 缩到 293 字节，完整二进制仅增加 56 字节到 1597288；零丢样 10K profile 的普通完整写路径不再出现 Future poll/constructor。源码契约、完整单元、33 项集成（含 `slow_reader.py` EAGAIN 路径）及不可变/current 50K 三类 guard 全部通过 | `src/server.uya`、`tests/integration/slow_reader.py`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`benchmarks/v0.9.3-release-performance-2026-08-02-writev-direct-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| AOF 中等记录缓冲可用：不超过 64KiB 的追加批次复用 64KiB 写缓冲，超过 64KiB 的聚合批次仍保持先 flush 后直写；520B 缓冲与 66KB 聚合直写均有单测，持久化/崩溃/损坏集成测试通过；两次 release 对比中 `SET 1KiB` 分别达到 2737/2955 req/s，后续 ACL 热路径优化阶段的正式报告为 2811 req/s | `src/persistence/aof.uya`、`tests/unit/persistence_aof_test.uya`、`tests/unit/test_runner.uya`、`make test`、`tests/integration/persistence_aof.py`、`tests/integration/persistence_rdb_aof.py`、`tests/integration/persistence_crash_matrix.py`、`tests/integration/persistence_corruption.py`、`benchmarks/v0.9.3-release-performance.md` |
| ACL 授权热路径优化可用：默认用户和 named user 维护分类拒绝规则计数，无分类规则时不再线性扫描生成命令目录；标准执行器依赖统一请求入口完成命令/key/channel ACL 门禁，避免重复校验。完整单测与 redis-py/redis-cli/命令 introspection 集成通过；正式 `PING` 从 9563 提升到 19428 req/s，`SET 16B` 从 8256 提升到 15980 req/s，独立确认轮的 `SET 16B` 达到 Redis 的 `1.18x/1.10x` | `src/network/connection.uya`、`make test`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`tests/integration/command_introspection.py`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| maxmemory 淘汰口径与复制缓冲分层可用：复制 backlog 和进行中的 AOF rewrite buffer 不参与 keyspace 淘汰预算，内部通过 `maxmemory_excluded_bytes` 调整预算，`INFO memory` / `MEMORY STATS` 以 `mem_not_counted_for_evict` 暴露；内存压力测试按 `used_memory - mem_not_counted_for_evict` 设置真实 keyspace headroom，noeviction、allkeys LRU/LFU、volatile LRU/LFU/TTL 与连续压力场景均通过，复制/AOF 集成语义不回退 | `src/replication/backlog.uya`、`src/server.uya`、`src/command/executor.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/maxmemory_pressure.py`、`tests/integration/maxmemory_noeviction.py`、`tests/integration/maxmemory_allkeys_lru.py`、`tests/integration/maxmemory_allkeys_lfu.py`、`tests/integration/maxmemory_volatile_policies.py`、`tests/integration/memory_info_stats.py`、`tests/integration/replication_psync_backlog.py`、`tests/integration/replication_incremental_sync.py`、`tests/integration/replication_consistency.py`、`tests/integration/persistence_aof.py`、`make test` |
| RESP batch 顶层容量按需增长可用：批量解析初始仅分配 4 帧，真实流水线超过容量时按倍数增长并保持 64 帧上限、半包和错误清理语义；6 帧扩容单测、完整单测和完整集成通过。maxmemory 策略测试余量同步按真实写入增长重新校准，不再依赖旧解析器的瞬时过度分配制造压力。正式 `SET 1KiB` 从 2811 提升到 6545 req/s、p99 从 1329us 降到 703us，独立确认轮达到 6884 req/s；后续 profile 中原 `resp_alloc_elements` 热点已低于 0.5% | `src/network/protocol.uya`、`tests/unit/network_protocol_test.uya`、`tests/integration/maxmemory_allkeys_lru.py`、`tests/integration/maxmemory_allkeys_lfu.py`、`tests/integration/maxmemory_volatile_policies.py`、`tests/integration/maxmemory_pressure.py`、`make test`、`make test-integration`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 空闲 MONITOR 快速路径可用：维护活动 monitor fd 计数，无订阅者时普通成功命令不构造、不转义、不分配监控行；注册去重，`RESET`、连接/事务关闭和失败发送统一清槽并防止计数下溢。完整单测、真实双连接 MONITOR、redis-py/redis-cli 集成通过；正式 `SET 1KiB` 从 6545 提升到 14386 req/s、p99 从 703us 降到 177us，独立确认轮达到 15848 req/s 和 Redis 的 1.29x；follow-up profile 中监控行构造已低于 0.5% | `src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/monitor_smoke.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 空闲阻塞客户端扫描快速路径可用：server 仅在阻塞请求成功保存后增加 `blocked_clients`，成功重新调度、`CLIENT UNBLOCK` 和连接关闭时递减；计数为零时跳过 64 槽全表扫描。完整单测、blocking list/zset 与 CLIENT 控制集成通过；正式 `SET 1KiB` 为 15463 req/s、Redis 的 1.14x，带 perf 的 20K 请求从 10993 提升到 12278 req/s，follow-up profile 中阻塞扫描已消失 | `src/server.uya`、`tests/integration/blocking_list_smoke.py`、`tests/integration/blocking_zset_smoke.py`、`tests/integration/client_config_smoke.py`、`make test`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 空闲 buffered-client 扫描快速路径可用：server 按 `input_len` 零/非零转换维护持有输入字节的客户端数，完整消费、连接关闭和阻塞请求回填保持计数同步；计数为零时跳过 64 槽全表扫描。慢速半包、流水线、blocking list/zset、CLIENT 暂停/解除和完整集成通过；该阶段正式 `SET 1KiB` 为 13874 req/s、Redis 的 1.02x，p99 为 142us 对 Redis 166us，相邻确认轮达到 15926 req/s 和 Redis 的 1.27x；带 perf 的 20K 请求为 11751 req/s，原 2.20% buffered 扫描已低于 0.5% | `src/server.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/slow_reader.py`、`tests/integration/blocking_list_smoke.py`、`tests/integration/blocking_zset_smoke.py`、`tests/integration/client_config_smoke.py`、`make test`、`make test-integration`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 无效回复配置占位优化可用：普通 `CommandReply`、零拷贝和批处理结果在 `request_config_set=false` 时使用零初始化 `ServerConfig`，不再逐命令构造完整默认配置；真实 `CONFIG SET` 仍从当前运行时配置复制、更新并传播完整结果。PING 零占位、CONFIG SET 全字段单测、完整集成、redis-py/redis-cli 均通过；正式 `SET 16B/1KiB` 分别达到 Redis 的 1.18x/1.12x 且 p99 更低，profile 中 `config_default_config` 与 `copy_literal` 已低于 0.5% | `src/command/executor.uya`、`src/network/connection.uya`、`tests/unit/command_executor_test.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/redis_py_subset.py`、`tests/integration/redis_cli_smoke.sh`、`make test`、`make test-integration`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 单线程 async allocator 快速模式可用：Uya `std.async` 新增显式单线程绑定，直接复用进程内 allocator 指针；普通 setter 仍关闭快速模式并使用 pthread TSD，server 仅在既有单线程 epoll loop 中启用。完整单测/集成通过；正式 `PING/GET 16B/GET 1KiB` 分别收敛到 Redis 的 0.89x/0.98x/0.99x，`SET 1KiB` 为 1.17x 且 p99 更低；profile 中原 2.24% `_pthread_registry_find` 及 allocator TSD 链已低于 0.5% | `uya/lib/std/async.uya`、`uya/docs/uya.md`、`uya/docs/changelog.md`、`src/server.uya`、`make test`、`make test-integration`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 事件批次时间缓存可用：server 在维护批次、epoll timeout 计算前和 epoll 返回后刷新 `event_time_ms`，同一事件批次内的命令时间、`CLIENT PAUSE` 和客户端交互时间复用缓存，不再逐命令读取毫秒时钟；完整单测、完整集成与 redis-cli smoke 通过。正式 `PING` 从 20162 提升到 23180 req/s、达到 Redis 0.97x，五项绝对吞吐/p99 guard 全部通过；相同 20K `SET 1KiB` profile 中时钟调用栈样本从 40/707（5.66%）降到 32/667（4.80%），吞吐从 10959 提升到 11287 req/s | `src/server.uya`、`tests/integration/idle_client.py`、`tests/integration/client_config_smoke.py`、`tests/integration/slow_reader.py`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-18-async-single.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 1KiB 字符串值 arena 可用：1025B SDS payload 命中 1032B 专用 class，每个低于 64KiB 的 arena 页切分为 62 个块，单块释放回到专用 freelist，测试 reset 在无活跃块时整页释放；原有 16B 到 1024B class 上限和 `redis_free` 契约不变。65 块跨页边界单测、完整单测/集成/redis-cli 与正式发布矩阵通过；20K `SET 1KiB` profile 中 `find_chunk` 从 6.69% 降到 2.50%，`owns_ptr` 从 3.03% 降到 0.5% 报告阈值以下，采样吞吐从 11287 提升到 11621 req/s | `src/memory/allocator.uya`、`tests/unit/memory_allocator_test.uya`、`tests/unit/test_runner.uya`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-18-event-time.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 命令 runtime 元数据缓存可用：`RedisServer.runtime_info` 持久保存稳定配置，普通请求仅刷新客户端、复制、maxmemory 排除量、后台任务、lastsave 和拓扑等动态字段，连接层继续覆盖 RESP/NO-TOUCH；启动 `requirepass`、成功 `CONFIG SET` 与 `REPLICAOF` 切换均在配置写入后同步缓存。带密码 smoke 曾捕获启动参数绕过同步点并已由 `server_set_requirepass()` 收口；完整单测/集成/redis-cli 和发布门禁通过，20K `SET 1KiB` 从 11621 提升到 12853 req/s，原 0.61% `server_runtime_info` 低于 0.5% 报告阈值 | `src/main.uya`、`src/server.uya`、`tests/integration/smoke_tcp.py`、`tests/integration/client_config_smoke.py`、`tests/integration/replication_role_state.py`、`tests/integration/maxmemory_pressure.py`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-18-value-arena.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 空闲 AOF rewrite 大栈门控可用：release 反汇编确认 `server_run_pending_aof_rewrite_at` 慢路径栈帧约为 `0x502f8` 字节，空闲 GET profile 中 96.45% 的函数样本落在栈探测循环；server loop 现仅在 rewrite 已请求或进行中时调用状态机。完整单测/集成/redis-cli、AOF rewrite 与崩溃矩阵通过；同一 40K 键数据集的 60K GET profile 中 16B/1KiB 服务端周期分别下降约 5.0%/5.6%，原 2.15%/2.62% 热点均低于 0.4%，正式五项吞吐均超过同机 Redis 且门禁通过 | `src/server.uya`、`tests/unit/server_test.uya`、`tests/integration/persistence_aof.py`、`tests/integration/persistence_crash_matrix.py`、`tests/integration/smoke_tcp.py`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-18-runtime-cache.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 命令回复配置状态缩窄可用：普通 `CommandReply` 与 `ConnectionProcessResult` 不再内嵌完整 `ServerConfig`，稀有 `CONFIG SET` 把候选配置暂存在 persistent runtime，并仅在回复成功编码后提交；同一 pipeline 后续 `CONFIG GET` 可立即读取新值，`ConnectionOutputTooSmall` 保持旧配置，包含 `replicaof` 的多参数更新先提交完整配置再执行角色切换。release 生成结构中 `CommandReply` 从 1176B 降到 192B，`command_executor_reply` 栈帧从 `0x4a8` 降到 `0xd8`；匹配 GET 16B profile 周期下降 3.4%，GET 1KiB 中目标回复 helper 全部低于 0.4%。完整单测/集成/redis-cli 与正式绝对吞吐/p99 guard 通过 | `src/command/executor.uya`、`src/network/connection.uya`、`src/server.uya`、`tests/unit/command_executor_test.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/client_config_smoke.py`、`tests/integration/replication_role_state.py`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-19-rewrite-stack.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| backlog 物理压缩原地复用可用：SDS 支持重叠安全且保留容量的前缀丢弃，backlog 达物理压缩阈值后在现有缓冲内前移有效窗口，不再 `sds_range` 分配/释放新 SDS；`histlen`、first offset、连续 PSYNC slice 和 master offset 不变，容量高水位保留到 reset/free 并计入 `mem_not_counted_for_evict`。SDS 内容/终止符/容量测试和 backlog 2100 次长周期测试确认跨阈值零新增 allocator 分配；完整单测、完整集成、redis-cli 及 PSYNC/full/incremental/consistency 通过。固定 key 100K `SET 1KiB` 父提交对比从 12924 提升到 14218 req/s，server cycles 从 9.446B 降到 8.233B，`find_chunk` 从 8.44% 降到 0.10%，无丢样；正式绝对吞吐/p99 guard 通过 | `src/storage/sds.uya`、`src/replication/backlog.uya`、`tests/unit/storage_sds_test.uya`、`tests/unit/replication_backlog_test.uya`、`tests/unit/test_runner.uya`、`tests/integration/replication_psync_backlog.py`、`tests/integration/replication_full_sync.py`、`tests/integration/replication_incremental_sync.py`、`tests/integration/replication_consistency.py`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-19-reply-state.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 常见 RESP 命令数组借用解析可用：`RespParseResult.elements_owned` 显式区分 owning/borrowed 描述符，argc 不超过 3 的扁平 Bulk/Simple String 命令直接写入连接栈槽；更大 argc、嵌套/整数/复杂 RESP 类型回退现有 RESP2/RESP3 parser，所有返回路径按所有权统一清理。协议单测覆盖借用数据、零分配计数、容量回退，连接单测确认 `PING` parser 零 allocator 分配；完整单测/集成/redis-cli 通过。固定 CPU 200K `PING` 对父提交吞吐从 11304.7 提升到 11652.0 req/s，cycles 从 14.064B 降到 13.585B，instructions/branches 分别下降 2.3%/2.4%；正式绝对吞吐/p99 guard 通过 | `src/network/protocol.uya`、`src/network/connection.uya`、`tests/unit/network_protocol_test.uya`、`tests/unit/network_connection_test.uya`、`tests/unit/test_runner.uya`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-19-backlog-inplace.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| ACL 命令拒绝空表快速路径可用：默认用户和 named user 分别维护活跃命令拒绝规则计数，成功新增 `-command` 时递增、`+command` 删除时递减，重复新增、删除缺失规则不改变计数，`resetcommands`、用户重置和删除清零；计数为零时授权路径不扫描固定 deny 表，分类拒绝计数语义保持不变。默认和命名用户单测覆盖初始、添加、删除与重置转换，完整单测/集成/redis-cli 通过。固定 CPU 200K `PING` 对父提交吞吐从 11271.3 提升到 11543.5 req/s，cycles 从 14.093B 降到 13.902B，instructions/branches 分别下降 1.46%/2.38%；正式五项绝对吞吐、归一化吞吐和 p99 guard 全部通过 | `src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-20-resp-borrow.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 全小对象 class arena 可用：16B 到 1032B 的 Slab class 在 freelist 为空时按 block stride 动态切分接近 64KiB 的页，单块释放回到对应 class，页按进程高水位保留；逻辑统计 reset 与仅限 allocator 单测的整页 reset 分离，避免对象池保留指针失效。单元测试覆盖小 class 首次补页、缓存字节、复用计数、1032B 跨页边界和完整 reset；完整单测、33 项集成、redis-cli 与正式五项 guard 通过。同条件 20K 父提交对比中 `SET 16B` 从 14703 提升到 19558 req/s（+33.0%），p99 从 238us 降到 106us（-55.5%），RSS 从 144452KiB 降到 119784KiB（-17.1%）；当前五项相对 Redis 为 1.08x/1.29x/1.00x/1.34x/1.09x | `src/memory/allocator.uya`、`tests/unit/memory_allocator_test.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-23-small-slab-arena-20k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| ACL 完全开放用户快速路径可用：默认用户或 named user 没有 command/category deny 且开启 `allkeys/allchannels` 时，主命令入口只解析一次用户名并跳过三组详细权限检查；任一限制存在时仍执行原命令、key、channel 检查和 `NOPERM` 回复。频道受限时非频道 PING 仍允许的单测、完整单测、33 项集成和 redis-cli 通过；两轮 50K 父提交 A/B 均值中 SET16/GET16/SET1K/GET1K 分别提升 6.7%/4.0%/6.1%/4.0%，PING 绝对值 -1.4% 但 Redis 归一化比值 +0.3%，正式 50K 五项绝对吞吐与 p99 guard 全部通过 | `src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-23-acl-unrestricted-fast-path-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 隐式默认用户 ACL 选择快速路径可用：事务为空或 `username_len == 0` 时直接判断默认用户 deny command/category 计数和 `allkeys/allchannels`，不再构造当前用户名切片并比较 `default`；显式默认用户、named user 和任一受限默认状态仍走原详细权限路径。完整单测、33 项集成和 redis-cli 通过；两轮固定 CPU 200K `PING` 对父提交 `17f1f21` 的 instructions/branches/cycles 均值分别下降 4.0%/5.6%/3.1%，不可变与当前 50K 正式报告的五项吞吐和 p99 guard 全部通过 | `src/network/connection.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-24-acl-default-transaction-fast-path-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 空 `requirepass` 认证门禁快速路径可用：普通命令仅检查 NUL 终止 `requirepass` 缓冲首字节是否为零，不再构造并扫描完整密码切片；AUTH、HELLO AUTH、ACL 和 CONFIG 的完整密码读取与比较保持不变，启动密码和运行时 `CONFIG SET requirepass` 语义由既有单元/集成覆盖。完整单测、33 项集成和 redis-cli 通过；两轮固定 CPU 200K `PING` 对父提交 `eb04709` 的 instructions/branches/cycles 均值分别下降 0.69%/0.74%/3.73%，吞吐均值提升 2.41%，不可变与当前 50K 正式报告的五项吞吐和 p99 guard 全部通过 | `src/network/connection.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`make test-redis-cli`、`benchmarks/v0.9.3-release-performance-2026-07-24-auth-required-first-byte-fast-path-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| CRC64 slicing-by-4 可用：冷路径先生成基础表，再派生三组零字节推进表；热路径把四个输入字节与当前 CRC 低位合并后，通过四次独立查表折叠，保留 Redis CRC64 多项式、任意初始值和分段更新语义。测试覆盖已知向量、512 字节完整数据、0 到 512 全部前缀和全部分段边界与标量实现一致。两轮反向顺序固定 CPU 200K `GET 1KiB` 父提交 A/B 中普通墙钟吞吐提升 5.53%；`perf stat` 环境吞吐提升 1.39%、cycles 下降 1.64%、branches 下降 0.17%。完整源码契约、单元、33 项集成、redis-cli、DoD 和正式 50K guard 全部通过 | `src/util/crc64.uya`、`tests/unit/util_crc64_test.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`make test-redis-cli`、`bash scripts/verify_definition_of_done.sh`、`benchmarks/v0.9.3-release-performance-2026-07-25-crc64-slicing-by-4-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| runtime backlog 单次快照可用：每条命令刷新 `CommandRuntimeInfo` 时直接从同一 `ReplicationBacklog` 状态计算逻辑 history 长度、first offset 和 allocator 排除字节，逻辑长度只计算一次，不再调用三个小 helper 或重复读取 SDS 长度；active、空 history、disabled backlog、AOF rewrite 排除量语义保持不变。两轮反向顺序固定 CPU 200K `GET 1KiB` 父提交 A/B 中普通墙钟吞吐提升 4.74%、p99 从 85.5us 降到 76us；`perf stat` 环境吞吐提升 1.85%，cycles/instructions/branches 分别下降 3.64%/1.17%/1.22%，复采样中三个 backlog helper 均无样本。完整源码契约、单元、33 项集成、redis-cli、DoD 和正式 50K guard 全部通过 | `src/server.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`make test-redis-cli`、`bash scripts/verify_definition_of_done.sh`、`benchmarks/v0.9.3-release-performance-2026-07-27-runtime-backlog-snapshot-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| runtime replid 变更点缓存可用：`CommandRuntimeInfo.replication_replid` 在服务初始化、提升为 master 重建 backlog 和切换为 replica 禁用 backlog 时同步，普通命令不再复制稳定的 40 字节 replid；角色切换单测验证 replica 清空旧缓存、master 新缓存与 backlog 完全一致。两轮反向顺序固定 CPU 200K `GET 1KiB` 父提交 `e7b5f41` A/B 中普通墙钟吞吐提升 0.87%、p99 基本持平；`perf stat` 环境吞吐提升 0.90%，cycles/instructions/branches 分别下降 2.79%/2.06%/2.54%，复采样中 `config_copy_bytes` 与 `copy_literal` 均无样本。完整源码契约、单元、33 项集成、redis-cli、DoD 和正式 50K 整体 guard 通过；不可变轮 GET16 的 normalized 子项受抖动未过但绝对吞吐与 p99 通过，当前复跑五项子 guard 全部通过 | `src/server.uya`、`tests/unit/server_test.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`make test-redis-cli`、`bash scripts/verify_definition_of_done.sh`、`benchmarks/v0.9.3-release-performance-2026-07-27-replid-cache-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 2 x u64 字节等值比较可用：`bytes_eq` 保持每次最多读取一个完整 16 字节块，但通过 `@vector.load` 的 memcpy 语义装载为两个 u64 lane 比较，避免 Uya 1.0 C 后端为 16 x u8 mask 归约生成 128 字节栈帧；测试覆盖非对齐切片及 15/16/17/31/32/33 字节边界。两轮反向顺序固定 CPU 200K `GET 1KiB` 父提交 `eb04985` A/B 中普通墙钟吞吐提升 2.01%、p99 从 71.5us 降到 66.5us；`perf stat` 的 instructions/branches/cycles 分别下降 0.83%/0.65%/0.16%。完整源码契约、单元、33 项集成、redis-cli、DoD 和两轮正式 50K guard 全部通过 | `src/util/bytes.uya`、`tests/unit/util_bytes_test.uya`、`make verify-uya-source-contract`、`make test`、`make test-integration`、`make test-redis-cli`、`bash scripts/verify_definition_of_done.sh`、`benchmarks/v0.9.3-release-performance-2026-07-27-bytes-eq-u64x2-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 默认 master 复制维护角色门控可用：`server_run` 仅在 `role_replica` 时调用 full sync、incremental sync 和 heartbeat 三个状态机，函数内部角色/状态检查继续保留，`REPLICAOF` 切换与副本心跳语义不变。两轮反向顺序固定 CPU 200K `GET 1KiB` 父提交 `7bfd495` A/B 中普通墙钟吞吐提升 2.17%、p99 从 81.5us 降到 77.0us；`perf stat` 的 cycles/instructions/branches 分别下降 0.25%/0.36%/0.29%，零丢样复采样中三个目标函数均无样本。源码契约、完整单元和复制心跳专项集成通过，不可变与 current 50K 的绝对吞吐、p99 和总体 guard 均通过 | `src/server.uya`、`tests/unit/server_test.uya`、`tests/integration/replication_heartbeat.py`、`make verify-uya-source-contract`、`make test`、`benchmarks/v0.9.3-release-performance-2026-07-27-replication-role-gate-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| RESP2 Pub/Sub 模式判断调用点内联可用：普通命令入口直接短路判断事务非空、订阅计数非零且协议不是 RESP3，删除唯一调用的 29 字节 helper；RESP2 已订阅命令限制、允许 PING、退订计数归零和 RESP3 已订阅普通 GET 语义不变。两组相反顺序固定 CPU 200K `GET 1KiB` A/B 合并吞吐提升 2.34%、p99 基本持平；`perf stat` 的 instructions/branches 下降 0.17%/0.20%，release 符号检查确认 helper 与调用消失。源码契约、完整单元、TCP Pub/Sub smoke 通过，不可变与 current 50K 的绝对吞吐、p99 和总体 guard 均通过 | `src/network/connection.uya`、`tests/unit/network_connection_test.uya`、`tests/integration/pubsub_smoke.py`、`make verify-uya-source-contract`、`make test`、`benchmarks/v0.9.3-release-performance-2026-07-27-pubsub-mode-inline-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| Dict key 等值包装层删除可用：`dict_table_find_entry` 与删除碰撞链在 hash 命中后直接调用 `bytes_eq`，删除只做 SDS slice 转换并尾跳的 28 字节 helper；桶选择、碰撞遍历、主/rehash 表顺序和字节比较语义不变。当前 profile 的原始 `dict_table_find_entry` 占 1.94%；两组相反顺序固定 CPU 200K `GET 1KiB` 的 8 轮墙钟合并提升 0.67%，`perf stat` cycles/instructions/branches 下降 2.48%/0.09%/0.09%，release 符号检查确认 wrapper 消失。源码契约、完整单元以及不可变/current 50K 的绝对吞吐、p99 和总体 guard 均通过 | `src/storage/dict.uya`、`tests/unit/storage_dict_test.uya`、`make verify-uya-source-contract`、`make test`、`benchmarks/v0.9.3-release-performance-2026-07-27-dict-key-eq-inline-50k.md`、`benchmarks/v0.9.3-release-performance.md`、`benchmarks/v0.9.3-performance-analysis.md` |
| 运维、安全与可观测余量仍在后续执行期；TLS / 更完整 `CLIENT/CONFIG/INFO/SLOWLOG/LATENCY/MEMORY/MONITOR/MODULE` 的证据待进入对应批次后补齐，其中 `LATENCY` 当前已完成 `command` 事件历史、`latency-monitor-threshold` 门限、top-level 命令直方图与容器子命令直方图 partial，`MONITOR` 当前仅完成流式观测 partial，`MODULE` 当前仅完成空模块列表兼容子集，`CLIENT REPLY` 当前仅完成命令回复抑制子集，`CLIENT UNBLOCK` 当前仅完成阻塞 pop 等待解除子集，`CLIENT TRACKING` 当前仅完成连接级 flags/redirect/prefix 兼容状态，`CLIENT CACHING/NO-EVICT` 当前仅完成连接级标志兼容子集，`CLIENT NO-TOUCH` 已接入对象访问 touch 抑制 | 以 `docs/redis-uya-todo.md` 当前 `v0.9.3` 计划为准 |
